# 知识点清单：本周回顾（2026-08-19 ~ 08-25）

> 用途：周五整理分享素材时的**核对清单**。每个知识点带"一句话"+"关键词"+"参考位置"，
> 整理时对着展开即可。详细成文版见同目录 `知识点-01/02/03`。

---

## 主题一：偏函数封装在核心架构中的意义

| # | 知识点 | 一句话 | 关键词 / 参考位置 |
|---|---|---|---|
| 1 | 引擎应保持通用 | 核心循环是"通用机器"，不该认识具体业务 | agent-loop、内核稳定 |
| 2 | 判断标准 | 引擎原则上不污染，除非业界成熟做法也这样 | 参考项目对照（pi） |
| 3 | 偏函数 | `createTool(hooks)` 返回 `execute(args)`——创建时烙业务（闭包），执行时只收运行时参数 | 工厂函数、闭包 |
| 4 | 运行时参 vs 闭包参 | signal/onChunk 走执行参数，业务 hooks 走工厂参数——位置不同天然分离 | `lib/session/manager.ts`、`lib/tools/types.ts` |
| 5 | 三个收益 | 引擎干净 / 好测试（注入假 hooks）/ 支持每请求组装 | — |
| 6 | 通用本质 | 依赖倒置 / 控制反转 / 策略模式 / 插件架构是同一套路 | — |

## 主题二：todo 面板（方法核心 / 状态更新 / 与 toolCall 的关联）

| # | 知识点 | 一句话 | 关键词 / 参考位置 |
|---|---|---|---|
| 1 | todo 是会话事件 | todo 和消息同文件、同生命周期、可回放——不是独立存储 | DSH 走读 07、SessionEntry type:"todo" |
| 2 | 整表替换 | `todo_write` 每次发完整清单直接覆盖，幂等防模型漂移 | `lib/tools/todo/` |
| 3 | 三态 + 校验 | pending / in_progress（最多一个）/ completed；trim / 去重 | validateTodos |
| 4 | 一次调用两个出口 | ToolResult 回模型（决定下一步）+ onTodoWrite 推前端（面板刷新） | `app/api/chat/route.ts` |
| 5 | 刷新恢复 | done 帧带 `getLatestTodos()`（叶子回溯权威值） | `lib/session/store.ts` |
| 6 | 前端只读 + 关闭规则 | 只读展示；未完成强制可见、全完成才可关、按会话隔离 | Reasonix 式交互 |
| 7 | 坑：面板是模型行为的镜像 | 模型不调 todo_write，面板就不变（残留问题根因） | — |

## 主题三：上下文压缩（B2 ③ LLM 结构化摘要）

| # | 知识点 | 一句话 | 关键词 / 参考位置 |
|---|---|---|---|
| 1 | 为什么压缩 | 窗口硬上限 / 成本 / 注意力退化；压缩 = 重写交接单不是删 | — |
| 2 | 四家对照 | pi（独立请求+显式增量）/ codex（服务端压缩）/ Reasonix（缓存对齐+经济性）/ DSH（检查点格式） | `doc/02` 方案文档 |
| 3 | 10 个取舍 | D1-D10：独立请求/格式混合/显式增量/不抄 retainedTail/经济性/防泄漏/截断检测… | `doc/02` 第四节 |
| 4 | 三步实现 | prepare（纯计算可测）→ generate（调模型，null 降级）→ commit（落盘） | `lib/session/store.ts` + `lib/summarize.ts` + `route.ts` |
| 5 | 序列化 | 消息→文本、toolResult 截断 2000、工具参数只留 key 名 | `serializeConversation` / `formatToolCall` |
| 6 | 增量更新 | previousSummary + UPDATE prompt，信息链不断 | `lib/summarize.ts` |
| 7 | 存储模型决定设计 | 只追加不删 → 不需要 retainedTail（firstKeptEntryId 指针即可） | `buildContext` |
| 8 | 经济性检查 | 待压区域 < 400 token 不压（省下的不够抵消 API 成本） | Reasonix foldEconomics |
| 9 | fail-closed | 错误/空输出 → 不落半截摘要 | DSH 截断检测 |
| 10 | MOCK 降级 | 无 key 时降级拼贴，压缩失败不拖垮对话 | `route.ts` |
| 11 | 易混淆点 A | 摘要多条（每次压缩一条 entry），但上下文只看最新一条 | buildContext findLastIndex |
| 12 | 易混淆点 B | 被压数据还在（JSONL 只追加不删），buildContext 只是过滤视图 | appendEntry |

---

## 补充素材（可选的分享角度）

- **本周数字**：32 个 commit / 测试 16 文件 111 用例 / 精读 pi·codex·Reasonix·DSH 四家压缩源码
- **踩坑实录**：孤儿 tool 400（切点落 toolResult）/ bash 中文乱码（GBK 解码）/ DeepSeek 400 / 面板残留
- **方法论**：核心功能改造前"多方案对照 → 讲透取舍 → 确认 → 开工"（AGENTS.md 规矩）

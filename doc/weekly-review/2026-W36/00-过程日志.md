# 2026-W36 过程日志

> 存"git 记不住的过程"：思路、错误/卡点、决策（含为什么）、下一步。
> 写入时机：commit 时（主，带功能名+hash）/ 压缩前（应急）/ 周五盘点时（扫 .traces/ 兜底）。
> 格式：每条三五句话，能看懂就行。不存代码/commit 内容（在 git）、不存完整对话（在 .traces/）。
> 周归属：`2026-Wxx` 按 commit 发生日实测归属（ISO 周，周一为一周开始），不凭印象。

## 08-31 周一 · 功能：导航手册渐进式 + 新增参考项目 SOP（commit: 889ab18）
- 思路：用户问"新增参考项目是不是要对它的架构进行解析，方便下次精确查找" → 把导航手册从单文件改为渐进式文件夹（index.md 索引 + 每项目一个文件 + sop-新增参考项目.md）；SOP 第 0 步确认项目存在 → 架构解析 → 填导航卡 → 回填 → 验收
- 错误/卡点：① 一开始我把 C15 面板接入混进 SOP（registry/测试/基线），用户纠正"克隆/更新代码是我的事，AI 只负责读懂+记下" → 范围收窄为纯知识索引 ② 全局文档加 C16 排期行被用户否"不要增加全局 agents/plan 内容" → 撤销，机制自包含在导航手册文件夹 ③ PLAN §4 维护双份列表 → 改"单一事实源"（只留一行引用 index.md）
- 决策：① 渐进式文件夹：index.md（索引/速览/已读走读索引）+ 9 项目各一文件 + sop ② SOP = 第0步确认存在 → ETCLOVG 七层解析（E/T/C/L/O/V/G，理论坐标系 = Survey）→ 导航卡（含已读列如实标）→ 验收 ③ AGENTS.md 11.6 加入口（指向 SOP）④ 交叉验证写进验收清单（教训：路径存在 ≠ 功能属实）
- 下一步：AGENTS.md 11.7 业务模块模式（下一条 commit）

## 08-31 周一 · 功能：C15 开源仓库更新面板（commit: ac2c62f）
- 思路：用户提"参考项目按天迭代，手动 pull 不知道变了啥" → /repos 页面（左右布局）+ 手动点更新 = fetch + pull + LLM 总结新增 commit + ⭐重点标注；基线 .repo-updates/<项目>.json 增量总结
- 错误/卡点：① git log 参数拼接成字符串 → "not an integer"（execFile 参数数组逐个 push 修复）② codex fetch 60s 超时 → FETCH_TIMEOUT_MS 180s + 超时提示 ③ 页面等好久 → 两阶段加载（listRepos 即时 + checkRepos 渐进）+ CHECK_TIMEOUT_MS 5s ④ 重点标注 raw markdown → parseHighlights 全文扫描 + prompt 强制 numbered + 学习价值/使用价值两行 ⑤ 分支探测被用户否 → 分支是数据写进 registry（opencode=dev 不是 main）
- 决策：① 分支是数据不是探测（命令固定 fetch/log/pull，分支作参数）② 状态 = 进页面实时 git ls-remote（毫秒级，不下载对象）③ 总结落盘 workspace/更新日志/（人看材料不进 git）④ selectModel 下沉 lib/selectModel.ts（lib 不反向依赖 app）
- 下一步：lib 门面 + 业务模块模式（下一条 commit）

## 08-31 周一 · 功能：lib 门面 + 业务模块探索模式（commit: 2667a6a）
- 思路：用户问"lib 下怎么这么闭塞，我以为 agent 核心是 export 出去给别人组装的" → 发现 lib 无 index.ts 门面，app 深路径直连内部文件 → 加 lib/index.ts 统一出口；用户进一步想清楚"以后要基于 agent 核心探索更多落地实践" → repo 迁出 lib
- 错误/卡点：① 门面 import 与文件内旧 import 重复（TS2300）→ 删旧留新 ② isValidSessionId 门面漏导出 → 补 ③ vitest include 需覆盖新位置（app/api/** 已含，不用改）
- 决策：① lib/index.ts = agent 内核公共门面（引擎/类型/消息/模型/会话/工具/压缩/审批/配置）② AGENTS.md 11.7：业务模块不进 lib，放 app/api/<模块>/_lib/（_ 前缀非路由，先例 _pipeline）+ 页面 + services，删模块 = 删 3 处不影响内核 ③ repo 从 lib/repos/ 迁到 app/api/repos/_lib/ ④ lib/agent.ts 红线不变
- 下一步：外部评估文档沉淀（下一条 commit）

## 08-31 周一 · 功能：外部评估沉淀 + 知识点分册 + 周复盘（commit: 6f080c9）
- 思路：把未提交的评估/知识点/复盘文档收尾提交（外部评估 E1-E6 已核实的报告 + DSH 压缩/会话架构分册 + W35 周报复盘 + 检查点更新）
- 错误/卡点：中文文件名 add 失败（pathspec did not match）→ git -c core.quotepath=false 后按实际名 add
- 决策：① 文档按主题分批（评估/知识点/复盘/详案补记/检查点一次收尾）② 会话检查点记录 C15 + 导航手册 + 外部评估排期（B9-B15 排队）
- 下一步：本次 4 个 commit（889ab18/ac2c62f/2667a6a/6f080c9）收尾完成，工作区干净

## 08-31 周一 · 功能：吸收体检报告行动清单为 B16-B18 排期 + 删除评估源文档（commit: 1cfc787）
- 思路：用户问"外部评估为啥要留着 / PLAN 吸收了吗" → 核对发现体检报告自己的 7 项行动清单没进 PLAN（E1-E6/S1-S4 已进）→ 吸收为 B16-B18 排期，源文档按 PLAN 7.5"用完即删"删除（3 份，-386 行）
- 错误/卡点：① 实测确认 agent.ts/deepseekModel.ts 零测试是真实缺口（非误报；唯一误报是 E1 审批正则，git log -S 证实从未存在）② PLAN 吸收要逐条对，不能只收结论不收行动项
- 决策：① B16 引擎核心零测试补齐（lib/agent.ts + lib/deepseekModel.ts，FakeModel 基建现成）② B17 前端切会话竞态 + 渲染优化（体检报告行动②⑥）③ B18 测试质量细节 ④ 评估源文档用完即删
- 下一步：B16-B18 排队（⏳）

## 09-01 周二 · 功能：B16+B18 引擎零测试补齐 + 测试质量细节（commit: ba05a54）
- 思路：用户拍板先做 PLAN 唯一标"最高优先级"的 B16+B18 → 先对照 pi 引擎测试思想（agent-loop.test.ts 1610 行：脚本式假模型 / spy 工具 / 事件序列精确 toEqual / llmCalls 计数 / block 断言组合）定 seam 再写
- 错误/卡点：① 流式测试必须传 onDelta 才走流式路径（complete 用存在性分流）——3 个流式测试漏传，冒烟当场抓出（response.json() 解析 SSE 文本崩）② vi.hoisted 回调拿不到 import 的 fs（__vi_import_0__ TDZ）——只能 node 全局拼路径
- 决策：① lib/testing/fake-model.ts 共享 FakeModel（响应序列/handler 双模式 + calls 记录；断言"模型第 N 轮看到了什么"是比 pi llmCalls 更直接的超集）② 不测没实现的东西（terminate/并行/截断保护/模型抛错兜底）③ B18 弱断言三种形态全清（truncated 未断言 / if-return 早退 / 恒真断言）
- 下一步：C15 单测落地（用户报"repos 单元测试全部不通过"）

## 09-01 周二 · 功能：C15 repos 单测落地 + vitest alias 修复（commit: d7bdb32）
- 思路：用户跑 pnpm test 发现 api 下 repos 测试全挂 → 首跑即全挂（C15 单测写完没跑过的债）→ 3 个根因逐个修
- 错误/卡点：① vitest.config 缺 @/ alias（vite 不读 tsconfig paths；repos 测试第一批用别名）——alias 修复附带让沙箱 vitest 能跑了（EPERM 消失）② vi.mock 工厂 TDZ（顶层 const 被 hoist 到文件顶部）；vi.hoisted 也拿不到 import 的 fs → 固定路径 + beforeAll 重建 ③ git 测试缺 origin：update-ref 改 refs 被 pull 的 fetch 覆盖 → 裸仓库当远端 + 真实 push；普通仓库当远端被 git 拒 push ④ fastForward 测试语义错：本地领先是 Already up to date 成功，必须分叉才拒绝 ⑤ summarize.ts 截断提示死代码（slice 后长度恒 40）→ buildPrompt 加 totalCount 参数
- 决策：① 裸仓库 = 真实 remote 形态（测试用）② 测试逮住的是 bug 就修生产代码，不改测试预期 ③ 固定路径可复现优于 mkdtemp 随机性
- 下一步：全套 31 文件 263 用例全绿 + tsc 绿；B16/B18/C15 收尾提交完成

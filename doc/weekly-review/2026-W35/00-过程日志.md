# 2026-W35 过程日志

> 存"git 记不住的过程"：思路、错误/卡点、决策（含为什么）、下一步。
> 写入时机：commit 时（主，带功能名+hash）/ 压缩前（应急）/ 周五盘点时（扫 .traces/ 兜底）。
> 格式：每条三五句话，能看懂就行。不存代码/commit 内容（在 git）、不存完整对话（在 .traces/）。

## 08-27 周四 · 功能：技能库筛选 + 每周回顾工作流（commit: f7f3901）
- 思路：C:\Users\25082\.agents\skills 下 41 个技能，32 个来自 mattpocock/skills（2026-08-27 批量装），大多依赖 GitHub issue 工作流；我的环境是内部 GitLab、无 issue，所以 tracker 流水线整条失效
- 错误/卡点：① 以为 diagnosing-bugs 是"收尾"技能，其实是"修 bug 全程"，收尾是 code-review ② 起初把"分享"当输出环节，被用户点醒：分享是认知检验机制（自圆其说才是真懂，费曼学习法）
- 决策：① 移除 13 个技能到 skills-archive（依赖 tracker 的流水线 + 引路壳 + 低频工具），保留 27 个（为什么：无 issue 工作流 → tracker 流水线全部失效）② 写作三件套保留（为什么：周五分享成文要用）③ 建每周回顾工作流，周五为闭合点（为什么：周六→周四只积累不整理，周五一次检验）
- 下一步：周五把本周所学讲给同事

## 08-27 周四 · 功能：提交后补记（commit 时漏执行过程日志）
- 错误/卡点：定了"commit 时写过程日志"的规则，结果本次 commit（f7f3901）自己没触发——根因：① AGENTS.md 11.5 的⑧只笼统写"沉淀"，没单独点名"commit 时必须更新 00-过程日志 带 hash"；② 工作流.md 的规则靠指针发现，而指针指向周五回顾，不绑定每次 commit
- 决策：① 本次补记（当前条目）② 把"commit 时更新过程日志"从工作流规则提升为 AGENTS.md 11.5 ⑧ 的显式子项（触发强度从"回顾工作流"提升到"每次提交"）
## 08-27 周四 · 功能：B3 原语评估 + keys 纯函数层 + 删除确认就地化（commit: 05e1363）
- 思路：用户问"哪些原语组件值得封装"→ 逐组件精读三个准弹框（approval/confirm/ask-user）+ menu + CSS，先讨论定方案再动手
- 错误/卡点：① 我原判断"键盘逻辑重复 2-3 处、抽 useGlobalKeydown"被代码事实推翻——三处是不同性质（全局快捷键/输入提交/列表导航），只有 approval 是全局监听，1 个使用者不值得抽 ② 我判断"mask 重复度最高"也是错的——全站只有 confirm-dialog 一个 mask ③ 流程执行漏了 ⑦ code-review，被用户抓出补上（双轴子代理）④ 初版行内确认条缺 Esc 取消/role/聚焦，review 后修复
- 决策：① dialog/mask/useGlobalKeydown/select/collapse/stepper 全部不封装（判据 = 重复度 + 未来复用者，不是"看起来像"）② 删除确认从居中 mask 弹框改行内确认条（复用行内重命名模式），confirm-dialog 退役删除（-155 行）——全站零模态 ③ 封装 app/lib/keys.ts 纯函数层（isPlainKey/isEditableTarget，seam 收最小结构 → node 环境可测，B8 ① 许可）
- 下一步：B3 遗留——18 处 <12px 等宽字号核查、⑤ 精读 DSH packages/client/AGENTS.md（建议周五回顾 Phase 2）、SessionRow 抽取（review 气味，不紧急）
## 08-27 周四 · 功能：B3 验收口径②等宽标签 12px 全站收口（commit: 1fdc6e9）
- 思路：上一任务遗留的 19 处 <12px 等宽字号核查 → 逐处读上下文分类后确认全是信息承载元素（时间戳/键帽/徽章/提示/表头），无纯装饰豁免
- 错误/卡点：① 初始 grep 说 18 处，精确核查是 19 处（口径要先数准）② 批量 edit 遇同文件重复模式失败（message-row 两处 10px、trace-rail 多处 11.5px），改带选择器上下文锚定才成功
- 决策：① 19 处全提到 12px（用户先问"为什么要改 依据是什么"——口径是 08-26 用户自己定的 §10.5 验收线，本次是落地收口）② 教学点：口径 vs 实现偏差有两种修法——让实现达标 or 修订口径（若观感突兀改的是口径不是硬提字号）③ code-review 用自查替代双轴子代理（同构机械批量改动，双轴信息量为零，判据：review 成本 ≤ 改动复杂度才 fan-out）
- 下一步：B3 剩余——⑤ 精读 DSH packages/client/AGENTS.md（建议周五回顾 Phase 2）、SessionRow 抽取（review 气味，不紧急）、注释纸感词汇标注
## 08-27 周四 · 功能：注释纸感词汇清理（commit: 8e59318）
- 思路：B3 遗留的"注释纸感词汇标注"——纸感 08-26 已退役（globals.css 总纲），但 10 个文件的注释还停留在"走纸记录仪/纸墨语言"旧口气，会误导未来读者（含 AI）
- 错误/卡点：① 用户抓出我方案过度——"逐文件加一行标识"是重复噪音，拍板"同一核心信息重复 3 次以上 = 方案本身有问题"，收敛为"一处总纲 + 其余清干净" ② code-review 双轴子代理抓出我统计漏了 lib/（trace-fold.ts 核心词没清）+ 2 处同文件内漏改 + 注记数字失真（23/21 对不上）→ 补 5 处 + 修正为 26 处 ③ pwsh 中文输出乱码反复干扰排查（已知问题，换 read 工具解决）
- 决策：① 历史标注 = 一处总纲（globals.css）+ 其余清干净；保留 trace-rail:51 退役注记 ② 代码标识符（inkWidth/.stroke/pen-*）不动（Spec 轴确认令牌/标识符零触碰），"墨线"呼应 inkWidth 保留 ③ 清理范围必须含 lib/（教训：只 grep app/ 会漏）
- 下一步：B3 只剩 ⑤ 精读 DSH packages/client/AGENTS.md（建议周五回顾 Phase 2）
## 08-27 周四 · 功能：B3-⑤ 精读 DSH client AGENTS.md（commit: 00ec7c7）
- 思路：B3 最后一个任务的收尾——精读 DSH "Web client stack" AGENTS.md，产出对照表（15 区）+ 八道闸脉络 + 动机分析，并借回"新组件 checklist"
- 错误/卡点：① 本地 app.asar 打包归档读不了 client AGENTS.md，web_search 拿不到全文，用户指路 E:\agents-read\deepseek-harness 本地源码直接读 ✓ ② 用户"不理解他为什么这样写"——我把动机分析写进文档并标注"是理解不是官方声明，学到深处若不对请回来改"（活文档原则）
- 决策：① 对照结论 = 我们的五层约束方向同构已验证（Styling/Layering/ctx/折叠纯函数/目录），缺 3 样（组件测试"测什么"标准→B8 用、新组件 checklist→已借回 §10.9、导出最小化）② 核心认知 = "AI 写的代码是协作产物没有文化兜底，用规则把文化钉死；在 AI 会自由发挥的地方，把自由换成模板和报错" ③ 不适用项（slot 系统/模块图/多包依赖）不借——借了就是 Speculative Generality
- 下一步：周五回顾 Phase 2 可用知识点-04 当素材；B3 全部收尾 ✅


## 08-27 周四 · 功能：SessionRow 抽取（commit: c825f44）
- 思路：code-review Standards 轴气味"嵌套三目 + 多职责"落地——session-list 的 map 回调拆出同行目录 session-row.tsx，SessionList 215→83 行只剩"列表容器 + 状态持有者"
- 错误/卡点：无（等价重构，5 项交互逐项核对通过）
- 决策：① 行内状态（editingId/draft/pendingDelete）保持提父级——单值互斥设计（同一时刻只有一行编辑/删除），不下沉到 SessionRow ② SessionRow 只收 props 转发回调，唯一例外是确认条取消按钮 ref+聚焦（纯行内视觉行为）③ 回调命名区分语义：onRequestDelete（进入确认态）vs onConfirmDelete（真删）④ code-review 用自查替代双轴子代理（等价重构的最严格验证是逐项等价核对，比子代理读 diff 更直接）
- 下一步：B3 剩余——⑤ 精读 DSH packages/client/AGENTS.md（建议周五回顾 Phase 2）、注释纸感词汇标注
## 08-27 周四 · 功能：C14 @文件匹配计划（commit: 02ff679）
- 思路：用户提"代码编写时需要指定文件"→ 先加 PLAN 行 → 本地实读 codex/pi 源码调研成熟实现 → 写详案含 A/B/C 方案对比
- 错误/卡点：① 我最初拿"158 文件"说效率，被用户纠正——未来 C6 多工作区，真实场景是几百上千文件，当前项目样本无意义 → 重做实测（379 文件 7ms / node_modules 17k 文件 81ms）② 用户问"gitignore 涉及文件要不要过滤"→ 查证 codex 用 ignore::WalkBuilder + respect_gitignore + require_git(true)（git 语义），确认必须过滤 ③ **git add -A 把并行会话刚生成的 03-五维评估文件误带进 commit** → soft reset 拆出，只提交 C14 文件（教训：add -A 会带进工作区所有未跟踪文件，含别的会话产物；提交前 git status 应逐文件核对）
- 决策：① 方案 A（抄 Codex 全链路：@ 触发→模糊搜索→选中→mention 编解码注入）——学习价值最高/解决记不住路径痛点/源码可对照/注入确定；排除 B（无交互硬伤）、C（控制权给模型+多一次往返，留降级备选）② 符号用 @ 不写死（config FILE_MENTION_SIGIL）③ .gitignore 过滤是必须项（巨型目录 81ms 混进来就卡；排除后 600 文件 12ms）④ 效率解药 = 范围界定 + debounce ~200ms ⑤ 文件+文件夹都搜（codex FuzzyFileSearchMatchType = file|directory）
- 下一步：C14 排在 C13 斜杠命令之后做；详案 doc/plan/c14-file-mention.md 已备
## 08-27 周四 · 功能：E3 死代码清理 + E4 messageText 收敛（commit: b0057c3）
- 思路：外部评估（另一 agent 五维评估）发现 E1-E6 问题 → E1 已核实误报（git log -S 证实字符类正则从未存在）→ 做 E3（死代码）+ E4（messageText 重复）
- 错误/卡点：① E4 若"简单删一份"是错的——manager 版有独立职责（compactionSummary 处理 + 联合数组窄化坑，注释写明为什么不用 filter(isTextContent)）；正确做法 = 职责合并进 message.ts 唯一入口，manager 引用 + preview 压空格（分隔符按用途定：模型/摘要用换行保留结构，preview 用空格压单行）② 注释里写换行转义被拆成真换行，tsc 抓出 TS1127 → 已修（代码即约束第④层的活例）
- 决策：① E3：删 deepseekModel 死代码（7 行）+ store/route 两处孤立空注释 ② E4：messageText(message: AgentMessage) 统一处理四种消息类型，flatMap 窄化替代 filter 类型守卫 ③ preview 处 replace 换行压空格保单行
- 下一步：E2 route.ts 拆解（与 B5 联动，大工程）、E5/E6 归入已有项；评估文档保持 untracked 用完删
## 08-27 周四 · 功能：E2 route.ts 阶段管线重构（commit: 7f8728a + fad4128）
- 思路：外部评估发现 route.ts 615 行"上帝组装器"不可测（E1 误报就是代价）→ 用户拍板完整拆解（不是只抽审批）→ 参考 codex request_processors（38 处理器） + pi create-harness（组装点只接线）→ 阶段管线方案
- 错误/卡点：① Standards 轴误报 context.ts sessionId 行为差异——git 历史证实原 route 一直传 3 参，误报无需修（教训：review 也可能误报，修前验证）② 用户抓出 hooks 拆两处可读性差（todo 藏 tools.ts 内部、askUser 从 route 传）→ 统一内部绑定（tools.ts 收 recorder 拿 runId）③ runPipeline 死参数（modelLabel/sessionId/sessionDir 签名撒谎）→ 双轴共识删 ④ 单测在 workspace-write 沙箱被拦（vitest EPERM）→ danger-full-access 才可跑 ⑤ write 工具"文件不存在"检查与 pwsh 删文件冲突 → 用 Set-Content 创建 body 绕过 ⑥ HMR 自动改 store.ts/context.ts → 还原防混入
- 决策：① route.ts 614→287 行，_pipeline/ 10 文件（frames/prompt/context/tools/model/approval/compact/ask-user/index）② approval 纯决策 + 15 单测（E1 教训解药：决策层可测）③ SSE 管道留 route 壳（send/abort/finalize 不进编排层）④ handleToolApproval/askUserApproval 留壳（决策已拆，副作用碰 send 该留壳，单使用端不抽）⑤ 落点地图：加功能→_pipeline 新文件+index 插行；加工具→lib/tools 新文件+注册行
- 下一步：E5/E6 归入已有项；PLAN 7.5 全部收尾（评估文档用完可删）









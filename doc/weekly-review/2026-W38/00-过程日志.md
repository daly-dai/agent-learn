# 2026-W38 过程日志

> 存"git 记不住的过程"：思路、错误/卡点、决策（含为什么）、下一步。
> 写入时机：commit 时（主，带功能名+hash）/ 压缩前（应急）/ 周五盘点时（扫 .traces/ 兜底）。
> 格式：每条三五句话，能看懂就行。不存代码/commit 内容（在 git）、不存完整对话（在 .traces/）。
> 周归属：`2026-Wxx` 按 commit 发生日实测归属（ISO 周，周一为一周开始），不凭印象。

## 09-14 周一 · 补记：本日早间 6 个 commit（当时 W38 日志未建，按 git log 回补索引）
- 说明：这一批发生在 W38 目录建立之前，漏了过程日志。此处只留一行索引，细节看各 commit message 与对应详案（不追补当时的思路，避免事后编造）。
- `3052804` docs(plan)：六个月跳槽计划详案 + 导航手册 codex 最新动向（09-14 侦察）
- `1ab875a` docs(plan)：C17 包装与开源推迟到迭代放缓（例外：ARCHITECTURE.md 已过期，属"已有的错"）+ C8 Skills 补入阶段 3
- `fac86cd` docs(readme)：README 重写（项目门面）
- `1cb3af6` feat(app)：B17 前端渲染优化收窄版（② TaskPanel key / ③ memo + foldEvents useMemo / ④ 决策区输入框常驻 + 覆盖层 / ⑤ 渲染平铺）
- `0675fa4` docs(agents)：11.8 —— 页面/UI 生成必须先调用 frontend-design skill（硬规则，不做"算不算"判断）
- `2a8bbb9` docs(plan)：A3 Trace Viewer 实现前补记（6 决策 + 8 验收 + 抄/不抄清单）+ C18 会话/压缩重构诊断（三档）

## 09-14 周一 · 功能：AGENTS 6.5 升硬规则（实现与单测同目录）+ C19 存量收编排队（commit: 9534e64）
- 思路：用户提"所有需要单测的文件和他的单测文件都需要放在一个文件夹下面 index.ts / index.test.ts" → 把 6.5 从「用户偏好」直接升为硬规则；顺手把 A3 的纯函数模块按新形态落位（`app/lib/trace-steps.ts` → `trace-steps/index.ts` + `index.test.ts`）
- 错误/卡点：① **旧 6.5 的措辞会让规则自我作废**——它写的是"新代码默认按这个结构组织"，而仓库里已有 20 处平铺 + 同名 `.test.ts`，按旧措辞这些永远合规、规则第一天就没牙 → 改成把约束挂在"**有单测**"这个触发条件上：没单测的文件（`format.ts` / `sse.ts` / `run-store.ts`…）不受约束，**补单测的那一刻**连同实现搬进文件夹；顺带禁掉 `xxx.ts` + `xxx/index.test.ts` 半拉子与 `impl.ts` 别名 ② 沙箱跑不了 vitest（vite 的 `optimizeSafeRealPathSync` 内部 `child_process.exec` 撞命名管道限制，EPERM，不是测试失败）→ 单测只能用户在本地 `pnpm test` 验；`tsc --noEmit` 沙箱可跑（exit 0）
- 决策：① 6.5 = 硬规则（目录化 + `index.ts`/`index.test.ts` + 外部 `@/…` 引用零改动，只改被移动文件自己的相对 import）② **C19 存量收编不当时做**：迁移会碰满 `lib/`（含正要改的 `lib/session/store.ts`），和 A3 的功能改动混进同一个 commit 会让评审失真 → PLAN 7.3 排「A3 完成后一次性做」③ C19 只写排期行、**不开详案**（纯机械迁移，做法在行里就说完了，再开文件只多一处要同步的地方）
- 下一步：A3 第三步 —— `lib/trace.ts` 加 `sessionId` + `readTraceMeta()`（只读首末行，供 run 列表用），然后 `app/api/traces/` 两个路由、`app/components/trace-viewer/`、`page.tsx` 顶部 tab（对话/轨迹），最后才摘掉 `trace-rail` 的渲染接线

## 09-14 周一 · 功能：A3 数据层形状定案 + C18 新增子项⑥「按工作目录分组」（commit: 28de244）
- 思路：用户问"我们未来的 session 会按分组、分目录存放吗"→ 去精读 DSH `session-persistence-jsonl/src/format.ts`，**先纠正了自己写错的形状**（见下）；顺带想明白分组对 trace 侧几乎无收益（文件名前缀已经解决了"列某会话的轨迹"），真收益是"删会话 = 删目录"和 C6 之后的多项目隔离
- 错误/卡点：① **我把 DSH 的形状写成了一层**——`sessionDir()` 实际给每个会话一个**目录**：`<root>/<projectKey(cwd)>/<encodeSegment(sessionId)>/session.jsonl`，注释写着 "available for future session-local artifacts"；PLAN 与详案都已纠正 ② 用户点出"会话也没分组，你现在开始 trace 分组了"——我提方案 B（按会话分目录）时**没先看 `.sessions/` 的形状**（平铺、53 文件、0 子目录）和 `.approval.jsonl` 这个"同会话文件族用**文件名**表达"的现成先例 → 方案 B 当场作废，改方案 C ③ 实测现存 39 个会话的 `cwd`——**只有一个值**（`workspaceRoot` 今日是全局单值）→ 分组现在做完只会看到一个项目目录，像白做
- 决策：① trace 形状 = **方案 C**（`.traces/<sessionId>.<runId>.jsonl`），**归属写「结构」不写「字段」** ② 分组并进 C18 成子项 ⑥，**由它把 A 档打开**（A 档的"`list()` 只读文件头"与分组要"列目录 + 读 `cwd`"是同一段代码）③ **甲乙形状（会话=文件 vs 会话=目录）暂不拍板，等 C6 工作区选择开工时一起定**——分组与 C6 是一对 ④ 编码强度判据抄 DSH：**项目名可有损**（撞了 = 两个项目混一个目录，`cwd` 还在头里可区分）、**会话 id 必须单射**（撞了 = 覆盖别人的会话）；顺带解释我们的白名单 `isValidSessionId` 够用是因为 id 出自自家 `generateSessionId()` ⑤ 分组开工前先花半小时把**4 处会话路径构造**（`manager.sessionPath` / `export/route.ts` / `_pipeline/context.ts` / `approvalLogPath`）收成 1 个模块——现在就有 3 处是重复的
- 下一步：A3 第 3 步 —— `lib/trace/` 单文件格式与读写

## 09-14 周一 · 功能：A3 数据层施工（轨迹折叠 + 按会话归属 + readTraceMeta）（commit: ac795db）
- 思路：按 11.5 走——seam 先定（折叠纯函数收 `TraceRun[]`、定位模块收"一个目录"），再写代码；顺势把 `lib/trace.ts` 搬进目录（6.5 新规则：**有单测就必须是 `index.ts` 同目录**）→ 外部 `@/lib/trace` 引用**零改动**，搬家这件事顺带把 6.5 选 `index.ts` 的回报验证了一遍
- 错误/卡点：① ⭐ **连续两次栽在"我脑子里的隐含假设"**：(a) 上一轮 `message_end` 用例——我以为"一轮 = 一条助手输出记录"，实际是"**一条助手消息 = 一条记录**"；(b) 本轮 `endLine` 测试助手默认塞了 `messages: 3`，于是"只有 error、没有 messages"这个组合**根本无法表达**，报错 `expected 3 to be undefined` 看起来像实现漏了字段。**两次修法都不是改断言迁就，而是让那个组合变得可表达**（上次把 kinds 数组显式断言出来，这次把助手的默认值拿掉）——共同点是：**没有任何一条用例去表达"我以为不可能"的那个组合**，所以假设才能藏住 ② 沙箱跑不了 vitest（vite 内部 spawn 撞命名管道 EPERM），单测只能用户本地跑
- 决策：① `readTraceMeta` 必须**真·部分读**（首行读 1KB + 尾行走 `fstat` 读 2KB）——`readFile` + `split("\n")[0]` 是最容易写出来的**假实现**（1MB 照样全读进来了），测试里用"中间塞坏 JSON"来区分两种实现（全量 parse 会抛）② `completed`（文件写完了）与 `summary.error`（这次运行跑得怎么样）是**两个语义**——崩掉的 run 是"`completed: true` + `error`"，别混成一个状态 ③ ⭐ **施工中发现第 4 个真问题并顺手接上**：`route.ts` 的 catch 原来只把错误发给 SSE、**不记轨迹**，而 `finally` 照写 `trace_end` → **崩掉的 run 与成功的 run 在轨迹文件里长得一模一样**，且 `runPipeline` 自己没有 try/catch（`_pipeline/` 只 `recorder.record(AgentEvent)`）→ 抛异常型失败在轨迹里**零痕迹**，而 A3 验收第 3 条正是"能定位哪一步开始错"。修 3 行（`let runError` → catch 记下 → `end({ messages, error })`），同时让 `error` 从"**预留字段**"变成有使用路径的字段——不接的话本次提交就等于又造了一个 `header.version` / `TraceSnapshot`
- 下一步：A3 第 4 步 —— `app/api/traces/` 两个路由（同时补 `lib/index.ts` 门面导出：等真有消费者再加，不预挖空壳）

## 09-14 周一 · 功能：A3 读取路由 + 内核门面导出（commit: 73dd648）
- 思路：数据层三层就位后，路由只是薄薄一层装配（列表 = 定位 + 逐个 meta；详情 = 定位 + 全读）。门面导出**现在才加**，因为消费者终于出现——上一轮特意不加，就是不想预挖空壳
- 错误/卡点：① **冒烟第一轮列表全空**——造夹具时用 PowerShell `Set-Content` 写"多行"，结果**整份文件没有换行**（6 行被空格连成了一行，PS 把数组按 `$OFS` 拼成了一个字符串），`readTraceMeta` 读不到首个 `\n` → 按设计返回 `undefined` → 列表为空。**是我的夹具坏了、不是代码**；而且这个表现恰好证明"坏文件不挂掉整个列表"这条设计在生效 ② 中途一度怀疑 BOM（PowerShell 5.1 的 `-Encoding utf8` 确实带 BOM），**假设被证伪**——改 ASCII（无 BOM）后仍然空。教训：**别靠猜，把代码编译出来直接跑**——`tsc lib/trace/index.ts lib/trace-files/index.ts --rootDir lib --outDir .tmp-check --module commonjs` + node 跑产物（沙箱里 node 不能跑 TS，这是既定绕法），一次就定位到"文件里根本没有换行" ③ 第二次冒烟 `curl "…/$rid?sessionId=…"` 又挂——**PowerShell 把 `$rid?sessionId` 当成一个变量名**（未定义 → 空串），URL 变成 `/api/traces/=s_smoke`，路由正确地回了 404；用 `${rid}` 才对。**本轮第三次栽在"我的工具/假设"而不是代码**（前两次：`message_end` 语义、测试助手默认值）
- 决策：① 详情路由的安全**靠结构不靠校验**——runId 绝不拼进文件路径，先 `listTraceFiles` 列出、再按 runId 匹配，路径来自 readdir 而非 URL → **路径穿越在结构上不可能**（对比 `chat/export` 的 sessionId：那个必须拼进路径，所以只能靠 `isValidSessionId` 白名单）。**能用结构挡住的，就别靠校验** ② `readTraceMeta` 返回 `undefined` 解读为"**这个文件不是轨迹**"——是**分类**不是失败，直接不列；崩掉的 run 走另一条路（文件是轨迹但 `completed: false`），**必须列出来** ③ 详情路由**先读 meta 再读 entries**：文件名像轨迹、内容却不是时，在读 1MB 之前就掉头
- 下一步：A3 第 5 步 —— `app/components/trace-viewer/`（**动手前必须先过 `frontend-design` skill**，AGENTS 11.8 硬规则）；形态已在 `workspace/prototype-a3-trace-viewer.html` 走查过一轮；已定的视图决策：顶部 tab（对话/轨迹）、composer 保持常驻、竖轴记录表、只看错误 / 下一个错误

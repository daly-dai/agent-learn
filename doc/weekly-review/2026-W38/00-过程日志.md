# 2026-W38 过程日志

> 存"git 记不住的过程"：思路、错误/卡点、决策（含为什么）、下一步。
> 写入时机：commit 时（主，带功能名+hash）/ 压缩前（应急）/ 周五盘点时（扫 .traces/ 兜底）。
> 格式：每条三五句话，能看懂就行。不存代码/commit 内容（在 git）、不存完整对话（在 .traces/）。
> 周归属：`2026-Wxx` 按 commit 发生日实测归属（ISO 周，周一为一周开始），不凭印象。

## 09-14 周一 · 补记：本日早间 6 个 commit（当时 W38 日志未建，按 git log 回补索引）
- 说明：这一批发生在 W38 目录建立之前，漏了过程日志。此处只留一行索引，细节看各 commit message 与对应详案（不追补当时的思路，避免事后编造）。
- `3052804` docs(plan)：六个月跳槽计划详案 + 导航手册 codex 最新动向（09-14 侦察）
- `1ab875a` docs(plan)：C17 包装与开源推迟到迭代放缓（例外：ARCHITECTURE.md 已过期，属"已有的错"）+ C8 Skills 补入阶段 3
- `fac86cd` docs(readme)：README 重写（项目门面）
- `1cb3af6` feat(app)：B17 前端渲染优化收窄版（② TaskPanel key / ③ memo + foldEvents useMemo / ④ 决策区输入框常驻 + 覆盖层 / ⑤ 渲染平铺）
- `0675fa4` docs(agents)：11.8 —— 页面/UI 生成必须先调用 frontend-design skill（硬规则，不做"算不算"判断）
- `2a8bbb9` docs(plan)：A3 Trace Viewer 实现前补记（6 决策 + 8 验收 + 抄/不抄清单）+ C18 会话/压缩重构诊断（三档）

## 09-14 周一 · 功能：AGENTS 6.5 升硬规则（实现与单测同目录）+ C19 存量收编排队（commit: 9534e64）
- 思路：用户提"所有需要单测的文件和他的单测文件都需要放在一个文件夹下面 index.ts / index.test.ts" → 把 6.5 从「用户偏好」直接升为硬规则；顺手把 A3 的纯函数模块按新形态落位（`app/lib/trace-steps.ts` → `trace-steps/index.ts` + `index.test.ts`）
- 错误/卡点：① **旧 6.5 的措辞会让规则自我作废**——它写的是"新代码默认按这个结构组织"，而仓库里已有 20 处平铺 + 同名 `.test.ts`，按旧措辞这些永远合规、规则第一天就没牙 → 改成把约束挂在"**有单测**"这个触发条件上：没单测的文件（`format.ts` / `sse.ts` / `run-store.ts`…）不受约束，**补单测的那一刻**连同实现搬进文件夹；顺带禁掉 `xxx.ts` + `xxx/index.test.ts` 半拉子与 `impl.ts` 别名 ② 沙箱跑不了 vitest（vite 的 `optimizeSafeRealPathSync` 内部 `child_process.exec` 撞命名管道限制，EPERM，不是测试失败）→ 单测只能用户在本地 `pnpm test` 验；`tsc --noEmit` 沙箱可跑（exit 0）
- 决策：① 6.5 = 硬规则（目录化 + `index.ts`/`index.test.ts` + 外部 `@/…` 引用零改动，只改被移动文件自己的相对 import）② **C19 存量收编不当时做**：迁移会碰满 `lib/`（含正要改的 `lib/session/store.ts`），和 A3 的功能改动混进同一个 commit 会让评审失真 → PLAN 7.3 排「A3 完成后一次性做」③ C19 只写排期行、**不开详案**（纯机械迁移，做法在行里就说完了，再开文件只多一处要同步的地方）
- 下一步：A3 第三步 —— `lib/trace.ts` 加 `sessionId` + `readTraceMeta()`（只读首末行，供 run 列表用），然后 `app/api/traces/` 两个路由、`app/components/trace-viewer/`、`page.tsx` 顶部 tab（对话/轨迹），最后才摘掉 `trace-rail` 的渲染接线

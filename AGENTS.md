# AGENTS.md

这个项目是「做中学」的 AI Agent **harness** 项目。**它不是生产系统**（不需要多租户 / SLA / 合规），**但它是作品**。

> **1. 代码可读性 > 功能完备 > 性能与炫技。**
> **2. 判据**：**玩具** = 「我能跑通」；**作品** = 「**别人能跑通** + **知道它什么时候会失败**」。

⚠️ **变的是判据，不是目的** —— 目的仍然是**学会 agent**，只是"怎么才算学会"从"我自己能跑通"换成了"别人能跑通"。**所以下面第 0 条的教学纪律（复盘 / 讲透 / 确认理解）一条都没变**，变的是它服务的判据。

## 写给 AI 编码助手（也写给未来的我）

0. **作品才算学会（最重要）**：用户要的不是"功能做完、commit 收工"，而是**真的学会**——而"学会"的判据是**作品**（**别人能跑通 + 知道它什么时候会失败**），不是"我自己能跑通"。每个功能做完后：
   - **主动复盘**：带用户回顾"这个功能到底教了什么"，梳理核心概念、关键设计决策、踩过的坑。
   - **确认理解，不急着 commit**：不要机械地问"要不要提交"——commit 是教学收尾的一步，不是终点。先确认用户掌握了这个知识点，再谈提交。
   - **教学 > 代劳**：用户问"为什么/怎么做的"，要讲清机制和取舍（"抄谁、为什么抄、不抄会怎样"），而不是只给结论。做中学的价值在"想明白"，不在"代码跑通"——**但"想明白"的最终证据是作品：别人能跑通、且知道它什么时候会失败**。
   - **⚠️ agent-loop 是红线（2026-08-24 用户明确要求，2026-08-26 加固为文件级，2026-09-01 路径随目录化更新）**：**红线文件 = `lib/agent/index.ts`（引擎核心循环 + `RunAgentLoopOptions`/`runAgentLoop` 签名）。凡 git diff / 暂存列表出现该文件——哪怕只改一行注释——必须先精读 pi 的对应实现 → 对比 → 展示结论 → 由用户判断，不允许直接修改、不允许"顺手微调"。** 引擎是"内核"，内核的任何改动都比新功能更需要先对齐参考项目（pi）。三条执行纪律：
     - **触发不看"算不算"**：diff 里出现 `lib/agent/index.ts` 即触发，不做"这次改动是否涉及 loop"的主观判断——文件级红线没有模糊地带。
     - **禁止顺带**：红线文件的改动必须**独立成一个 commit**，禁止混在功能 commit 里"悄咪咪"带过；混入时整个 commit 停下来单独走流程。
     - **收尾自检**：commit 前 `git diff --name-only` 扫一遍，出现红线文件必须已走完"精读→对比→展示→确认"流程，并在展示清单里**显式标注**"本 commit 含 lib/agent.ts 改动"。没有先和 pi 对照、没有经过用户确认，绝不改 engines。

1. **先想清楚再动手**：改结构前先读 `PLAN.md` 和 `workspace/` 下的笔记；新功能先在 `PLAN.md` 补一段思路，再写代码。
2. **函数小而单一**：一个函数只做一件事；超过约 30 行就拆。
3. **命名说人话**：变量/函数名要能读出意图，禁用 `a`、`tmp`、`x1` 这类名字。
4. **注释解释「为什么」，不解释「做什么」**：代码本身已经说明"做什么"，注释只写"为什么这么设计""这里有个什么坑"。少写注释比写满注释好。
5. **避免**：深层嵌套、长链式调用、魔法数字、一句话塞多个逻辑、为了少几行把可读性搭进去。
6. **分层别乱**：`lib/` 是内核（协议 / 引擎 / 工具 / 模型适配），`app/` 是产品层（API / UI）；provider 字段只允许出现在适配层。
6.5. **实现与单测同目录，且都叫 `index`（2026-09-01 定，2026-09-14 强化为硬规则，不设例外）**：**凡是有单测的文件**，实现与单测必须放在**同一个文件夹**下，文件名固定 `index.ts` + `index.test.ts`（需要 jsdom 时用 `index.test.tsx`）。
    - 形态：`app/lib/<模块>/index.ts` + `app/lib/<模块>/index.test.ts`；对齐 `lib/tools/` 每工具一目录的既有先例。
    - **为什么**：单测测的不是"那个文件"，而是"那个模块"——入口固定叫 `index.ts` 之后，模块的 public 面（导出）与它的测试是一对一、看得见的；也避免 `foo.ts` 与同目录别的实现文件混在一起、实现与测试交错。
    - **外部引用不变**：`@/app/lib/<模块>` 照样解析到 `<模块>/index.ts`，只改模块内部对外的相对路径。
    - **没单测的文件不受约束**：可以继续平铺成 `app/lib/xxx.ts`。但**一旦给它补单测，实现要连同测试一起迁进文件夹**——禁止出现 `xxx.ts` + `xxx/index.test.ts` 这种半拉子形态，也禁止 `xxx/` 里叫 `impl.ts` 之类的别名。
    - **⭐ 自查判据（2026-09-14 起成立）**：由本规则推出一条**一眼可查的不变量**——「**不叫 `index.test.ts` 的测试文件数应为 0**」。谁都能用一次文件名搜索核对，不需要懂实现。**当前状态：43 个测试文件 / 违规 0**（2026-09-14 C19 收编 20 处后达成；此前是 20 处违规）。**这就是规则该有的形态：不靠记性，靠一个能查的数。**
7. **标识符用英文，注释可用中文**：这是中文项目，中文注释欢迎，但代码命名保持英文。
8. **每步可运行**：改完要能 `tsc --noEmit` 通过。
9. **提交先展示、经确认后才可执行**：**禁止未经展示直接 `git commit`**。提交前必须先把「改了哪些文件、关键代码怎么改、为什么这么改」讲清楚，用户明确确认后才能提交。这是**作品**，提交即教学材料——绝不能绕过用户（AI 提交的每一行，用户都要看得懂）。**提交服务于学习：先确认用户理解了这步的"为什么"，把提交当成一次教学回顾的收尾，而不是先 commit 再解释。**

10. **计划维护流程（2026-08-26 定，跨会话一致性）**：PLAN 改动频繁、且每次开发都是新会话——**流程规则写在这里（AGENTS.md 每次会话自动加载），不依赖"记得去读 PLAN"**。三件事必须按流程走：
    - **新增计划项**（用户想到新功能 / 讨论后加入）：先在 `PLAN.md` 7.3 排期表加一行（状态标 `💡 讨论中` 或 `⏳ 排队`），**讨论定案后**再新建 `doc/plan/<名>.md` 详案（功能描述 + 实施方案 + 验收标准）。讨论结论（含"为什么不做"）也要记录，方便回顾。
    - **已有计划升级**（实施中发现更成熟方案，当前实现是"够用版"，更成熟的是"完全体"）：**在原详案文件追加补记**（保持时间线：原方案 → 触发原因 → 新方案 → 决策），不要新建文件、不要覆盖原文。同功能演进都记在一个文件里，回顾时打开一个文件全看到。
    - **收尾必做**（每个功能做完、commit 前）：① 更新对应 `doc/plan/*.md` 详案（实现细节/实测发现的坑）② 更新 `PLAN.md` 7.3 状态列（✅ 完成）③ 更新 `会话检查点.md`（现状/决策/给下一个我）④ 展示改动清单等用户确认再提交。**少一步都算没做完。**

11. **每周回顾工作流（2026-08-27 定）**：周五是回顾分享日。周四晚用户说「本周回顾」/「开始周报」时，按 `doc/weekly-review/工作流.md` 执行 Phase 1（盘点）+ Phase 2（深化）；周五用户自己讲出来（核心检验，agent 只兜底质询）；分享后把「讲通的那一版」固化成技术文档再发布。触发口令不写死，用户说「回顾」「周报」等同义说法都可触发。

11.5. **执行 PLAN 的标准流程（2026-08-27 定，开发主线的操作纲要）**：① 讨论/学习/定方案（同时写清 seam：该功能的 public 接口在哪、测什么）→ ② 写单测（只测 ① 商定的 seam，纯函数/工具层先行）→ ③ 写代码（每步 `tsc --noEmit` 可运行）→ ④ 写完再学习（讨论到搞明白；没懂就回 ③）→ ⑤ 验收（测试 + tsc）→ ⑥ 改 bug（先写能复现的测试再改，修完回归）→ ⑦ code-review（双轴：规范 + 是否实现需求）→ ⑧ 沉淀（详案 / PLAN 状态 / 检查点 / **`doc/weekly-review/2026-Wxx/00-过程日志.md`——commit 时必写，带功能名 + hash**）→ ⑨ commit（commit 动作即触发 ⑧ 的过程日志写入）。**灵感旁路（贯穿全流程，不阻塞主线）**：聊的过程中冒出新功能/想法 → 当场判定「核心 or 完善」→ 核心排 A/B 阶段、完善排 C 或阶段末、判定不了标 `💡 讨论中` → 更新 `PLAN.md` 7.3 排期表加一行 → 回到主线继续（捕获即走，绝不让新想法打断当前功能）。详细 skill 触发映射见 `doc/weekly-review/工作流.md`。

    **② 写单测的夹具规则（2026-09-14 加，来自同一类错误在一会话内连犯三次）**：
    - **硬约束：手造的夹具必须注明「这个状态为什么真的会发生」。** 写不出这句话，就说明在造一个真实数据里不存在的状态——那测的不是实现，是你自己的假设。
    - **先问**：夹具里凡是我自己写下的常量（`= []`、`= 3`、手拼的对象字面量），都是**我注入的输入**。它从哪来？真实数据里是几？
    - **优先推导 → 其次必填 → 最后才默认**：能让夹具从已有数据**推导**出该字段就推导（非法状态就**造不出来**）；退一步让它**必填**（调用者必须表态）；最差是**给默认值**——等于悄悄注入一个假输入，而且报错还指不到真因。
    - **能走真管道就走真管道**：夹具由真实生产者产出（先例：A3 用真 `TraceRecorder` 写 → `readTrace` 读 → `foldTrace` 折），与实现同源 → 不可能不一致。
    - **单个夹具不超过十来行**：夹具短，人扫 diff 时能一眼看穿；200 行的对象字面量会把这类 bug 藏到进仓库之后。**这条是给人审用的**——这类错的最后一道防线是第二双眼睛，不是 AI 的自觉。
    - **反面教材（2026-09-14，同一会话连犯三次）**：① 心里默认"一轮 = 一条助手输出记录"（`expected 4 to be 3`）② 助手 `endLine` 默认塞 `messages: 3`，让"只有 error"这个组合**无法表达**（`expected 3 to be undefined`）③ 助手 `fold()` 默认 `turns = []`，造出"4 条带轮次的记录、却没有一轮"的 fold（`expected turns: 0`）。**三次报错都指不到真因**（失败发生在跟被测逻辑无关的地方）；两次修法也都不是"改断言迁就助手"——② 拿掉默认值，③ 改成从记录推导。

11.6. **新增参考项目流程（2026-08-31 定）**：用户说「新增了一个参考项目 A」时 → 读 `doc/开源项目导航手册/sop-新增参考项目.md` 并照做（第 0 步确认项目存在 → 架构解析 → 写 `A.md` → 更新 index 索引 → 同步 PLAN §4）。**克隆/更新代码是用户自己的事**，AI 只负责"读懂 + 记下"（解析架构、建立"想学什么→去哪找"的路径索引）。

11.7. **业务模块探索模式（2026-08-31 定）**：lib/ 是 **agent 内核**（引擎/模型/会话/工具/类型，公共门面 = `lib/index.ts`，外部从这里 import，不深路径直连内部文件）。**业务模块**（基于内核探索的落地实践，如 C15 仓库更新面板）**不进 lib**，放 `app/api/<模块>/_lib/`（`_` 前缀 = App Router 非路由，先例 `_pipeline`）+ 页面 `app/<模块>/` + 客户端 `app/services/<模块>/`。删一个业务模块 = 删这 3 处目录，不影响 agent 内核。内核红线不变：`lib/agent/index.ts` 改动仍需 pi 对照 + 人工确认。

11.8. **页面 / UI 生成必须先调用 `frontend-design` skill（2026-09-14 用户明确要求，硬规则，不设例外）**：**凡涉及页面、组件、样式、布局的新建或改造——哪怕只改一处视觉——动手写代码前必须先 `skill(frontend-design)` 加载设计指导**。不做"这次算不算页面改动"的主观判断（对齐 0 条红线纪律的"触发不看算不算"）。
    - **为什么**：AI 没有审美兜底，默认产出会聚类成三种"AI 味"模板（暖米色底 + 高对比衬线 + 陶土色 / 近黑底 + 单一荧光强调色 / 报刊式细线密栏）。skill 提供的是**显式设计方向 + 自审流程**：先出 token 系统（4–6 个命名色值）与 ASCII 线框 → 对照 brief 自审"这块是不是任何同类页面都会这么写" → 改掉 → 才写代码。
    - **⚠️ 加载 ≠ 遵守：动手前必须先读「已有设计输入」（2026-09-14 加，来自一次真实翻车）**。skill 的流程是"想清楚再画"，但它**明确要求**"用上你做过的设计作为提示"——**只看 skill、不看已确认的原型，就会在真空里设计**。所以加载 skill 之后，按顺序读这四样，读完再出提案：
      1. **已确认的静态原型** `workspace/prototype-*.html` —— 形态已拍板，是**基准**不是参考
      2. `app/globals.css` 的 token 表 —— 色值 / 字号 / 圆角 / 命中区，**不许自创**
      3. `app/components/` 下同族组件 —— §10.6：组件组合 > 发明
      4. `doc/plan/*.md` + `会话检查点.md` 里**已拍板的视图决策**（顶部 tab / composer 常驻 / 错误不加背景色…）
    - **提案必须逐条标注「继承」与「改动」**：「**继承**：原型的什么」/「**改动**：原型的什么，为什么原型不够」。**没有对照就没有改动权**——"我觉得这样更好"不算理由，要说出原型在**哪个具体场景**下不成立。报告里没有这两栏，就等于没读输入（**这是一个能被检查的信号**，不靠事后抓）。
    - **反面教材（2026-09-14，A3 轨迹视图）**：加载了 skill 却没读 `workspace/prototype-a3-trace-viewer.html`，凭记忆提出"**墨迹轴**（轴按记录类型着色、颜色突变 = 出错位置）"。而**原型自己的教学案例当场证伪了它**——T2 的 `grep "foo"` 返回 No matches、绿色、无报错，却是**真正的根因**；红色出现在无辜的 T4。同轮还提了"出参折叠"，而原型是**常驻**入参/出参摘要（注释写着「根因往往就在这」）。**三处提案全部撤回**。教训：**调用 skill 完全合规，但产出物跟已批准的原型打架——这个破绽规则拦不住，只能靠人抓。**
    - **与 §10.6 五层约束的关系（互补，都要走）**：skill 管**视觉方向**（palette / typography / layout / signature）；§10.6 管**工程约束**（令牌 / 组件组合 > 发明 / 文档 / 代码即约束 / 流程人审）。
    - **与 §10.7 教训一致**：设计身份是**用户的需求，不是 AI 的发明**——skill 的产出是**提案**，仍须用户拍板；用户有权随时丢弃。
    - **新形态先给看**：现有组件库覆盖不到的新布局（如记录表 / 检查器），**先出静态形态让用户确认，再接数据**——避免写完再推倒。
    - **落地位置**：skill 在用户级目录 `C:\Users\25082\.agents\skills\frontend-design`（跨项目可用）；若要项目内固化，可 vendor 进仓库 `.agents/skills/`。

11.9. **页面复杂度控制（2026-09-14 用户要求，两条规则）**：页面膨胀时只有**三条出口**，判据各不相同——先判断属于哪一类，再动手。

    **核心分界（最重要的一条）**：**hook 管「状态与副作用的绑定」，纯函数管「数据变换」。** 不含 React 状态/副作用的东西**就不该是 hook**——它该在 `app/lib/` 里当纯函数（node 环境可单测，不用 jsdom）。范本：`run-fold.ts`（纯折叠）+ `run-store.ts`（zustand 桶）+ `use-agent-run.ts`（薄投影）三层分工。

    **① 页面超过 500 行 → 拆组件**
    - 拆出的组件放**独立文件夹**：`app/components/<组件名>/index.tsx` + `index.module.css`（同行目录的 `session-row.tsx` 是轻量例外，新拆默认独立文件夹）
    - **怎么切**：按"**能一句话说清它负责什么**"切。两种先例——可复用单元（`SessionRow`）/ 独立状态分支（`UpdateBody`，B17⑤ 平铺时抽出）
    - 拆出的组件**通过 props 收数据**、不碰全局（§10.9 第 4 条）
    - ⚠️ **500 是触发线不是目标**：不到 500 行但"一个文件里两件事"就该拆；超过 500 行但逻辑单一（如一个长 switch）可以留

    **② 状态耦合 → 抽 hook；纯计算 → 抽纯函数**
    - **该抽 hook 的三种情况**：① 多份 `useState` 互相牵动（改 A 必须同时改 B）② 一个 state + 绑定的副作用（订阅/监听/定时器，**带清理**）③ 预期被第二处复用
    - **不该抽的三种情况**：① 只是单次计算 → 用 `useMemo` ② **不含 React 状态/副作用 → 该是 `app/lib/` 的纯函数** ③ 只有一个消费者的一次性 adapter（§10.8 已判过："一个 adapter = 假设的 seam"）
    - **实用信号**：一个 `useState` 只被一个 handler 用、且不影响别的状态 → **不抽**；两个以上 state 互相牵动 → **该抽了**
    - 先例：`use-command-menu`（状态机，page 550+→336 行）/ `use-sticky-scroll`（状态 + 副作用，页面只剩两行组装）

## 给人类（你自己）

- 分层、路线图、功能映射看 `PLAN.md`。
- 系统提示词在 `app/api/chat/route.ts` 顶部，想调 agent 的说话方式就改那里。
- 轨迹（调试黑匣子）在 `.traces/`，一个 run 一个 jsonl 文件。

## 常用命令

```bash
pnpm dev                                        # 开发（浏览器 localhost:3000）
node node_modules/typescript/bin/tsc --noEmit   # 类型检查（沙箱里 pnpm 被拦时用这个）
```

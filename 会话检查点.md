# 会话检查点（Checkpoint）—— 2026-08-25 更新版

> 这是开发会话的"压缩摘要"。**新会话/上下文恢复后，读这份 + `PLAN.md` + `AGENTS.md` 即可接上，不必重读历史对话。**
> **新会话必读清单（按顺序）**：
> 1. `PLAN.md`（路线图 + 各阶段实现前补记 + **13.3 完整排期 A1-A5/B1-B7/C1-C12**）
> 2. `workspace/做中学学习主线.md`（**学习课程表**：每阶段学什么/做什么/读哪篇/验收/考核）
> 3. `workspace/三项目横向对照-施工决策表.md`（每个阶段项抄谁 + **第五节工具参考源**）
> 4. `workspace/工具全景对比-7项目.md`（**2026-08-24 新增**：7 项目工具全景 + 缺口排期）
> 5. `workspace/精读走读-01~18-*.md`（17 篇核心走读 + 产品端面板对照，需要时查对应篇）
> 6. `workspace/理论核心-ETCLOVG落地.md`（七层坐标系，全局视角）
>
> 学习沉淀（旧）：`workspace/知识点学习记录.md`、`workspace/Node异步机制演示.md`、`workspace/踩坑实录.md`。
> **2026-08-25 新增**：`doc/01-agent-loop-纯净性改造.md`（方案 B 定稿：业务 hooks 工厂闭包烙入、引擎零业务）。

---

## 项目现状（Phase 0-5 完成 ✅ + A4 提交 + agent-loop 纯净性改造完成）

- 架构：Next.js (App Router) + TypeScript。三层：`lib/`（内核）+ `app/`（产品层）+ 模型适配层。
- 完整链路：真实 DeepSeek（流式 + 真实 token 用量）→ ReAct 循环 → **11 个工具**（含人工确认弹框）→ 会话记忆 → SSE 流式 → 前端观测台。
- **2026-08-25 agent-loop 纯净性改造完成**：`RunAgentLoopOptions`/`ToolExecutorOptions` 删掉 `onTodoWrite`/`onAskUser` 业务字段；业务 hooks 改走**工厂参数 + 闭包烙**（`createTodoTool(hooks)`），引擎只传运行时参（signal/onChunk）——完全贴 pi（`create-harness.ts:35`）。`lib/tools/types.ts` 新增 `ToolHooks`。
- **2026-08-25 compaction 孤儿修复**：`compactIfNeeded` 切点原来可能落在 toolResult 上（它配对的 assistant toolCall 被压进摘要 → DeepSeek 400 "tool must be response to tool_calls"）。修法：切点前移 while 循环。`lib/sessionStore.test.ts` 3 用例。
- **2026-08-25 config 整合**：新建 `lib/config.ts`（全项目配置唯一入口）：provider 可扩展（每厂商自带 contextWindow/reserveTokens，压缩阈值随 provider 走，DeepSeek 1M 窗口）、paths/approval/bash 收编。`shouldCompact()` 对齐 pi（`token > 窗口 - 预留`）。`lib/config.test.ts` 5 用例。
- **2026-08-25 bash 乱码修复**：`chcp 65001` 对内建命令写管道不可靠（实测 dir 仍 GBK）。新建 `lib/process-output.ts`（TextDecoder gbk + stream 模式，跨 chunk 半个汉字正确拼接），bash-runner 改用它。`lib/process-output.test.ts` 6 用例。
- **2026-08-25 确认弹框超时**：60s → 5 小时（`APPROVAL_TIMEOUT_MS`），前端删除时间文案。
- git 历史（已提交 18 个 commit，Phase 0-5 + A4 + agent-loop + 配置）：
  ```
  75a6d27  docs: PLAN 补记 agent-loop 纯净性改造规划
  2f4fc75  feat(ui): 通用确认弹框替换 window.confirm
  669ba4f  docs: 学习优先规则 + A4 规划补记 + 忽略项整理
  9de50d5  feat(A4): ask_user_question 多问题选项版（无超时 + run 清理）
  ...（此前 13 个 Phase 0-5 commit）
  ```

## 精读/规划成果（workspace/ 笔记**不进 git**，检查点本文件**进 git**）

> 2026-08-25 调整：`会话检查点.md` 移到根目录并纳入版本管理——以后每个需求改完，**检查点 + PLAN.md 按需更新并随代码提交**（用户规矩）。workspace/ 下其余笔记仍不进 git。

| 类别 | 文件 | 状态 |
|---|---|---|
| 三项目精读 | `精读走读-01~17-*.md`（pi 7 课 / codex 5 课 / DSH 6 课） | ✅ 完成 |
| 产品端对照 | `精读走读-18-产品端面板对照.md`（tether + Reasonix） | ✅ 完成 |
| 决策矩阵 | `三项目横向对照-施工决策表.md`（抄谁 + 分歧裁决 + **第五节工具参考源**） | ✅ 完成 |
| 学习课程表 | `做中学学习主线.md`（学什么/做什么/读哪篇/验收/考核） | ✅ 完成 |
| 理论核心 | `理论核心-ETCLOVG落地.md`（七层坐标系 × 我们 × 走读 × 阶段） | ✅ 完成 |
| 项目对比 | `开源项目对比分析.md`（9 项目定位谱系 + 六维对比） | ✅ 完成 |
| 规划 | `三项目精读路线图.md`（阅读顺序 + 施工单 + 完成总结） | ✅ 完成 |
| **工具全景** | **`工具全景对比-7项目.md`（2026-08-24：7 项目工具名实读 + 缺口三档 + 排期）** | ✅ 完成 |

**精读结论**：核心抄 pi/codex/DSH，产品端 UI 抄 tether/Reasonix。规划已闭环，**接下来主线是"做"不是"读"**。

## 排期全景（2026-08-24 更新，PLAN.md 13.3）

> ⚠️ **以 `PLAN.md` 13.3 为准（单一事实来源），此处仅概览快照，可能过期。** 排期变化只改 PLAN.md，改完顺手同步本表。

```
A 主线收尾：A1 Phase5✅ → A2 vitest → A3 Trace Viewer → A4 ask_user_question → A5 web_search
B 工程化：  B1 审批 → B2 真摘要 → B3 原语库 → B4 diff → B5 hooks → B6 记忆 → B7 终端加固
C 功能丰富：C1 MCP → C2 沙箱 → C3 子代理 → C4 schedule → C5 遥测 → C6 工作区 → C7 设置
           → C8 Skills → C9 代码执行器 → C10 goal → C11 complete_step → C12 move/read_image
非核心可选：LSP 代码智能 / tool_search（条件触发，不排期）
```

> **2026-08-24 用户拍板的原则**：**"除非非核心功能，其余都应该安排到 plan 里面"**——之前"暂缓/不学"的措辞是 AI 建议、用户未拍板（用户质问"我什么时候说不学的"），已全部修正为排期；仅 LSP/tool_search 标"非核心/可选"。

## 模块地图（2026-08-25 agent-loop 纯净性改造后）

- `lib/`：`types.ts`（协议 + SessionStats + **TodoItem/SessionEntry.todo**）、`config.ts`（**2026-08-25：全项目配置唯一入口，provider 可扩展**）、`model.ts`（TeachingModel）、`deepseekModel.ts`（真实适配器，流式 tool_call 分片累积）、`mockModel.ts`、`agent.ts`（引擎，**只传运行时参 signal/onChunk**）、`message.ts`、`sessionStore.ts`（JSONL 会话 + appendTodo/getLatestTodos + **compactIfNeeded 切点修复**）、`sessionManager.ts`、`trace.ts`、`toolApproval.ts`、`runControl.ts`、`userAnswers.ts`（A4 挂起提问）、**`process-output.ts`（2026-08-25：子进程输出解码器，TextDecoder gbk + stream）**
- `lib/tools/`：一工具一目录——`index.ts`（ToolRegistry + 组装，**createToolRegistry(ws, hooks)**）、`types.ts`（**ToolHooks + ToolExecutorOptions 只含运行时参**）、`path-utils.ts`、`shared.ts`、`list/read/write/write-note/edit/delete/grep/find/bash/todo/ask-user/`（各 index.ts + index.test.ts）、`bash/bash-runner.ts`
- `app/api/`：`chat/route.ts`（SSE 主路由 + 审批 + **每请求组装 registry + hooks 闭包**）、`chat/approve/`、`chat/stop/`、`chat/ask-user/`（A4 回传）、`sessions/route.ts`
- `app/components/`：`nameplate` `overture` `message-row` `trace-rail` `session-list` `approval-dialog` `task-panel` **`ask-user-card`**（A4）**`confirm-dialog`**
- `app/lib/`：`sse.ts`（帧类型，含 tool_todo / ask_user_request）、`use-agent-run.ts`（含 todos/pendingAsk）、`use-sessions.ts`、`trace-fold.ts`、`format.ts`、`messages.ts`
- `doc/`：`01-agent-loop-纯净性改造.md`（方案 B 定稿：业务 hooks 工厂闭包、引擎零业务）

## 关键决策（确认过的，别再翻案）

1. **人工确认**：写/改/删 + bash **全量弹框**（`TOOLS_NEEDING_CONFIRM`），**5 小时超时自动拒绝（2026-08-25 从 60s 放宽，前端不展示时间文案）**；secret/秘密 硬拦；**"会话/全局允许"留待 B1**。
2. **bash 超时 30 秒**；停止走 `POST /api/chat/stop`。**2026-08-25：输出解码用 `lib/process-output.ts`（GBK + stream），不再 chcp 65001**（对内建命令写管道不可靠）。
3. **统计 = 会话级累计**（服务端从会话文件算），不是前端临时值。
4. 统计语义、UI、三元风格都听用户（"简单的三元保留、嵌套的拆"）。
5. **提示词「任务优先于教学」**：执行/验证先给结果，失败如实报错误+退出码。
6. 提交规矩（AGENTS.md 第 9 条）：**先展示清单 → 用户确认 → 提交**；`git add` 后必须 `git diff --cached --name-status` 验证暂存列表。
7. **Phase 5**：todo 是**会话事件**不是独立存储（`SessionEntry.todo`）；`todo_write` **整表替换**（幂等）；**2026-08-25 改**：工具不碰 store，hooks 走**工厂参数 + 闭包烙**（`createTodoTool(hooks)`，贴 pi），不再经引擎透传；**不进弹框**（低危）；前端**只读展示**。
8. **agent-loop 纯净性（2026-08-25，红线）**：引擎（`RunAgentLoopOptions`/`executeToolCall`）**不认识任何业务**；业务 hooks（onTodoWrite/onAskUser）走工厂参数闭包烙，引擎只传运行时参（signal/onChunk）。**改 agent-loop 必须先精读 pi → 对比 → 展示 → 用户判断，不许直接改**（AGENTS.md）。
9. **配置集中（2026-08-25）**：`lib/config.ts` 是唯一配置入口，引擎/工具不直接读 `process.env`；provider 可扩展（换厂商 = AI_PROVIDER + config 加一项 + 适配器），压缩阈值随 provider 的 contextWindow 走（对齐 pi shouldCompact）。**C7 设置页 UI 将来读写它**。
10. **压缩是"防爆窗"保险不是"勤打扫"**：DeepSeek 1M 窗口，阈值 = 窗口 - 预留（约 984K），几十上百轮才触发；演示压缩可临时调 env。
11. **工具排期原则（2026-08-24）**：默认都进 PLAN，非核心才不排；A4/A5/C9-C12 已排，LSP/tool_search 条件触发。
12. **流式 tool_call 机制**：arguments 是**分片累积**（`deepseekModel.ts`），流结束后一次 parse。**缺口：无截断保护**（`stopReason="length"` 时残缺 JSON 可能被 parse 成不完整对象去执行——pi 有 `failToolCallsFromTruncatedMessage`，我们没补，todo 靠 `validateTodos` 兜底）。

## 遗留 / 待办（"还有好多没实现"清单）

| 项 | 状态 | 位置 |
|---|---|---|
| **A2 测试框架（vitest）** | ✅ 完成（15 文件 98 用例） | lib/*.test.ts |
| **A4 ask_user_question** | ✅ 完成并提交（`9de50d5`） | PLAN.md 13.3 |
| **B2 真摘要压缩**——**进行中**：①摘要消息类型 `compactionSummary`（修"页面渲染奇怪"：摘要伪装 user 被当前端气泡）②切点不落 toolResult（✅ 已修）③LLM 结构化摘要 ④阈值配置化（✅ 已做，config.ts） | ⏳ 进行中（①待做） | PLAN.md B2 补记 |
| **A5 web_search / web_fetch** | 排队 | PLAN.md 13.3 |
| **A3 L3 Trace Viewer** | 排队 | PLAN.md 13.3 |
| **B1 审批升级**（只读放行 + 分级 + 日志；`TOOLS_NEEDING_CONFIRM` 可配化） | 排队（最快见效） | PLAN.md 13.3 |
| **C7 设置页 / 配置整合**——`lib/config.ts` 骨架已建（✅），设置页 UI 读写未做 | 排队 | PLAN.md C7 前置 |
| 流式截断保护（`failToolCallsFromTruncatedMessage`） | 待办 | deepseekModel.ts |
| 工作区选择（完整版） | 暂缓 | PLAN.md 十一节 |
| UI 视觉打磨（确认弹框样式，用户不满意） | 暂缓 | PLAN.md 十一节 |
| fetch 封装（`app/services` 已部分做） | 暂缓 | PLAN.md 十一节 |
| 思考过程渲染（`reasoning_content`，适配器未捕获） | 暂缓 | PLAN.md 十一节 |
| edit_file 模糊匹配 / diff 预览（B4） | 排队 | PLAN.md 13.3 |

## 环境与验证经验（沙箱边界，别再踩）

- **沙箱起不了常驻 dev server**（`next dev` fork 报 EPERM）——约 30 秒短暂窗口可 curl 冒烟；**浏览器交互验证必须用户跑 `pnpm dev`**。
- **node 不能直接跑 TS**；临时脚本：tsc 编译到临时目录 + node 跑产物；顶层 await 包进 `main()`。
- **BashRunner spawn+pipe 在沙箱里 EPERM**——测它要 `sandbox_permissions: "danger-full-access"`。
- **PowerShell 传中文 JSON 给 curl 必 400**——请求体写文件 + `curl --data-binary @file`。
- Windows：cmd 输出 GBK（bash **已用 `lib/process-output.ts` GBK 解码，不再 chcp**）；进程树要 `taskkill /t /f`。
- **子代理扫描残留**：调研时生成的 `.scan-*.txt` 等垃圾文件，用完即删。

## 未提交内容（工作区）

- **本轮（2026-08-25）未提交**：agent-loop 纯净性改造（types/tools/index/agent/route/测试）、compaction 切点修复 + sessionStore.test、process-output + 测试、确认弹框 5h 超时、config.ts 整合 + 测试、PLAN.md B2/C7 补记、`doc/01-*.md`（改造方案文档）
- **workspace 笔记全部未跟踪（用户决策：笔记不进 git）**：18 篇精读走读 + 学习主线 + 决策表 + 理论核心 + 对比分析 + 路线图 + 工具全景对比等
- **会话检查点本文件（2026-08-25 起进 git）**：移到根目录，随代码提交

## 常用命令

```bash
pnpm dev                                        # 开发（浏览器 localhost:3000）
node node_modules/typescript/bin/tsc --noEmit   # 类型检查（沙箱里 pnpm 被拦时用这个）
```

## 给下一个"我"的三句话

1. **架构状态**：Phase 0-5 + A4 已提交；**agent-loop 纯净性改造完成**（业务 hooks 工厂闭包、引擎零业务，贴 pi）；**config.ts 配置整合完成**（provider 可扩展、压缩阈值随窗口走）；compaction 切点修复 + process-output GBK 解码完成。下一步主线 **B2 ①摘要消息类型**（修"页面渲染奇怪"）→ A5 / B1。
2. **用户偏好**：可读性第一；UI 用户要自己调；提交前必须展示清单等确认；统计/产品语义先问用户；**"除非非核心功能，其余都排进 PLAN"**；**学习优先于产出**（AGENTS.md 第 0 条）：功能做完先复盘学到的知识、确认用户理解，别机械地问"要不要 commit"。**agent-loop 是红线**：改引擎必须先精读 pi → 对比 → 展示 → 用户判断。
3. **下一步**：B2 ①（摘要消息类型修渲染）正等着做——新增 `role:"compactionSummary"` 消息类型，buildContext 产出它，deepseekModel 转换时伪装 user（模型无感），message-row 渲染成折叠卡片（前端正常）。开工前先 PLAN.md 补思路 → 展示 → 确认 → 实现；**主线是"做"不是"读"**。

# 会话检查点（Checkpoint）—— 2026-08-26 更新版

> 这是开发会话的"压缩摘要"。**新会话/上下文恢复后，读这份 + `PLAN.md` + `AGENTS.md` 即可接上，不必重读历史对话。**
> **新会话必读清单（按顺序）**：
> 0. `AGENTS.md`（**每次会话自动加载**；**第 10 条计划维护流程 = 强制规则**——新增/升级计划项、收尾必做四步，都按它走，不依赖"记得"）
> 1. `PLAN.md`（**骨架版**：路线图 + 7.3 完整排期 A1-A5/B1-B7/C1-C13；详细施工方案在 `doc/plan/*.md`，开工对应项时才读）
> 2. `workspace/做中学学习主线.md`（**学习课程表**：每阶段学什么/做什么/读哪篇/验收/考核）
> 3. `workspace/三项目横向对照-施工决策表.md`（每个阶段项抄谁 + **第五节工具参考源**）
> 4. `workspace/工具全景对比-7项目.md`（**2026-08-24 新增**：7 项目工具全景 + 缺口排期）
> 5. `workspace/精读走读-01~18-*.md`（17 篇核心走读 + 产品端面板对照，需要时查对应篇）
> 6. `workspace/理论核心-ETCLOVG落地.md`（七层坐标系，全局视角）
>
> 学习沉淀（旧）：`workspace/知识点学习记录.md`、`workspace/Node异步机制演示.md`、`workspace/踩坑实录.md`。
> **2026-08-25 新增**：`doc/01-agent-loop-纯净性改造.md`（方案 B 定稿：业务 hooks 工厂闭包烙入、引擎零业务）。
> **2026-08-26 新增**：`doc/02-B2-LLM结构化摘要-改造方案.md`（四家对照 + 10 决策）、`doc/知识点-01~03-*.md`、`doc/知识点清单-本周回顾.md`（周五分享用）、**PLAN 渐进式加载拆解**（737→180 行，详案入 `doc/plan/*.md`、历史归档 `doc/plan-archive/*.md`；操作流程固化进 AGENTS.md 第 10 条）。

---

## 项目现状（Phase 0-5 完成 ✅ + A4 + agent-loop 纯净性 + B2 ③ LLM 结构化摘要完成）

- 架构：Next.js (App Router) + TypeScript。三层：`lib/`（内核）+ `app/`（产品层）+ 模型适配层。
- 完整链路：真实 DeepSeek（流式 + 真实 token 用量）→ ReAct 循环 → **12 个工具**（含人工确认弹框）→ 会话记忆 → SSE 流式 → 前端观测台。
- **2026-08-25 agent-loop 纯净性改造完成**：`RunAgentLoopOptions`/`ToolExecutorOptions` 删掉 `onTodoWrite`/`onAskUser` 业务字段；业务 hooks 改走**工厂参数 + 闭包烙**（`createTodoTool(hooks)`），引擎只传运行时参（signal/onChunk）——完全贴 pi（`create-harness.ts:35`）。`lib/tools/types.ts` 新增 `ToolHooks`。
- **2026-08-26 B2 ③ LLM 结构化摘要完成**（对齐 pi 三步：prepare 纯计算 → generateSummary 调模型 → commit 落盘）：
  - `lib/summarize.ts`：三提示词（系统/初始/增量 UPDATE）+ `serializeConversation`（toolResult 截断 2000、工具参数只留 key）+ `generateSummary`（失败返回 null 走降级）
  - `lib/session/store.ts`（原 sessionStore.ts 迁入 `lib/session/`）：`prepareCompaction`（找 prevCompaction→previousSummary、toolResult 前移、经济性字段）+ `commitCompaction`；`summarizeEntries` 拼贴降级；`buildContext()` 折叠视图（压过的原文不进上下文，与 pi/DSH 一致）
  - 10 决策：D1 独立请求 / D2 pi 格式+DSH 两段 / D3 显式增量 / D4 不引入 retainedTail（JSONL 只追加不删，firstKeptEntryId 定位）/ D5 切点按条数+toolResult 前移 / D6 经济性检查（<400 token 不压）/ D7 工具参数只留 key / D8 失败降级拼贴 / D9 空输出 fail-closed / D10 文件列表附摘要
  - 压缩前推 `{type:"compacting"}` 帧，前端显示提示条「上下文超出上限 正在压缩上下文，请稍候…」，避免用户以为卡住
- **2026-08-26 bash 乱码根治（受限语言模式）**：实测发现 Windows Smart App Control/WDAC 会把 pwsh 压进 ConstrainedLanguage，`.NET 类型创建`被禁 → 旧 preamble 双 pin 整行失败 → 输出仍 GBK。新方案：`cmd /c "chcp 65001 >nul & pwsh -EncodedCommand <base64>"`——chcp 是 cmd 内建不碰 .NET，pwsh 启动时读到 65001；base64 传命令避开 cmd 引号解析。受限/非受限都有效。
- **2026-08-26 实测修复**：① compaacting 文案改「上下文超出上限 正在压缩上下文，请稍候…」② todo 面板最后一项卡 in_progress → 提示词补硬规则（任务全部完成必须再调 todo_write 全标 completed）③ 压缩卡片 `<pre>` → `<Markdown>`（对齐 pi）④ 路线 A 定案：压缩后前端不展示被压原文（与 pi/DSH 一致，数据都在，以后随时可改）
- **2026-08-26 配置口子**：`lib/config.ts` 的 contextWindow/reserveTokens/keepRecentMessages/minCompactTokens/maxTurns 全支持 env 覆盖（CONTEXT_WINDOW/RESERVE_TOKENS/KEEP_RECENT_MESSAGES/MIN_COMPACT_TOKENS/MAX_TURNS）；`.env.local` 已恢复正常值（CONTEXT_WINDOW=1000000，MAX_TURNS 默认 16 已固化进 config.ts 并删除 env 覆盖）
- **2026-08-26 B1 审批升级完成**（详案 `doc/plan/b1-approval.md`，含实现补记）：
  - ① 只读命令放行：`lib/permission/readonly.ts` 词法静态分析 + fail-closed（走私语法/写参数拦截），18 测试
  - ② 分级模式：suggest/bypass/never 三档（抄 pi defaultProjectTrust + CodeWhale approval_mode），`lib/approvalMode.ts` 运行时可变 + `/api/chat/approval-mode` 接口 + 前端三档控件（参考项目全有运行时切换入口）
  - ③ 审批日志：`lib/permission/approval-log.ts` JSONL append + 成对校验（asked/decided，孤儿/重复报错），10 测试
  - ④ 前端面板 + 记忆：`lib/permission/approval-memory.ts`（session=内存 Map / persist=`.sessions/approval-rules.json`，8 测试）；面板抄 Reasonix **内嵌消息流卡片**（非模态）+ 快捷键 Y/A/P/N + bash 命令单独渲染 + 按钮独占一行；判定顺序 = 一直允许→本会话→只读→弹框
  - ④改（2026-08-26，用户要求"像 askUser 悬浮在输入框位置"）：审批卡从消息流**挪到底部决策区**（与提问卡共用 `decisionBar`，输入框隐藏、卡片占据其位置，钉在底部永远可见）；`scrollToEnd` 不再依赖 pendingApproval；`.askBar` 更名 `.decisionBar`；保留非模态 + 快捷键
  - 关键坑：卡片"浮在输入框上"= pendingApproval 变化要 scrollToEnd；bash 参数是 JSON 壳要提取 command 渲染；测试文案别嵌中文引号（TS1005）
- **2026-08-26 UI V2：尺度升级 + 溢出菜单（B3 启动，用户 UI 反馈触发）**（详案补记 `doc/plan/ui-design.md` §10.5）：
  - 用户反馈："UI 很小家子气，操作按钮很小，挤不下就放 dropdown，所有面板也是，参考 reasonix/dsh"。拍板：**34px 按钮 + 15px 正文**、**顶栏清空 + 会话行重命名/删除全进 ⋯ 菜单**
  - 令牌：`--r` 3→6 / `--r-lg` 5→10、正文 14→15px、新增 `--hit: 34px`；三栏 240/348 → 264/384、对话列 830→880
  - **底色 V2 二次调整（用户反馈"不喜欢这个背景颜色"）**：暖调米灰（#e9eae4 偏绿黄）→ **中性灰**（`--plate #fafafa` / `--paper #ececec` / `--recorder #e0e0e0`），`--line/--line-soft` 同步中性化；五支笔色不动（浅底调好的）
  - **底色 V2 三次调整（用户拍板"为啥一定要纸感"→ 纸感退役，走中性壳）**：`--plate #ffffff` / `--paper #f8f8f8`（阅读面，视觉即白）/ `--recorder #e4e4e4`（侧栏/轨迹明确灰，与白落差 20 单位）；**轨迹区点阵纸背景删除**（trace-rail 纯色面板）；五支笔色保留（功能语义色非装饰）；教训：设计身份是用户需求不是 AI 发明，§10.7 详记
  - **会话行 V2 三次调整（抄 DSH 会话列表）**：① ⋯ 菜单从常驻改回**鼠标移上才显示**（opacity + pointer-events，hover/focus-within 兜底，34px 命中区保留）② **不再显示 session 文件 id**：标题 = `title ?? preview ?? "未命名会话"`，重命名草稿只回填已有标题、占位提示"会话名称" ③ **⋯ 去边框**（幽灵按钮，可点性靠 hover 纸色底，点击面积不变）④ **弹框侧开**（`align="side"`，用户要求"偏右别挡列表"）：fixed 定位浮到侧栏右侧，避开 `.sessionList` 的 overflow 裁剪，滚动/缩放自动关
  - **清空会话功能删除（2026-08-26 用户拍板）**：Phase 1 单会话时代遗产，多会话下被「新建/删除」覆盖，且无确认弹框（静默抹历史，比删除还危险）；顶栏 ⋯ 菜单整个移除（页头零操作按钮）；服务端 `DELETE /api/chat` + `services.clearHistory` 保留（未来斜杠命令复用）；`use-agent-run` 删 reset（resetLocal 保留，切会话用）
  - **新原语 `Menu`（`app/components/ui/menu/`，B3 头一个）**：点外/Esc/Tab 关闭、打开聚焦首项、↑↓ 循环、危险项红、独立交互岛（stopPropagation）；顶栏「⋯」收清空记录、会话行「⋯」收重命名/删除（替换 hover 小按钮 ✎/×）
  - 高频操作放大：发送/停止 40px、新建会话实心 34px、审批按钮 42px、确认/提问 36px、状态灯 14px、读数盘 18px
  - 验收：全站命中区 ≥34px、等宽标签 ≥12px、零 hover 小按钮、tsc 通过
- **2026-08-28 B3 原语评估 + 删除确认就地化 + keys.ts 纯函数层**（详案 `doc/plan/ui-design.md` §10.8）：
  - **原语候选评估**（用户问"哪些原语值得封装"→ 逐组件精读后推翻两个候选）：`ui/dialog/`（唯一使用者 confirm-dialog 删除确认，改造后归零）、`ui/mask/`（全站只有 confirm-dialog 一个 mask，"重复度最高"是误判）、`useGlobalKeydown`（只有 approval 一个全局监听者）、select/collapse/stepper（无真实需求点）——**全部不封装**；判据 = 重复度 + 未来复用者数量，不是"看起来像什么"
  - **删除确认改造**：confirm-dialog（居中 mask 弹框）退役删除（-155 行），改**行内确认条**（复用行内重命名模式：状态替换行内容，点 ⋯→删除 后该行原地变红字确认条）——**全站从此零模态弹框**，mask 彻底出局
  - **`app/lib/keys.ts` 纯函数层**（+6 测试）：isPlainKey / isEditableTarget，从 approval-dialog 内联判定提炼；seam 收最小结构 → node 环境单测（B8 ① 许可，vitest include 加 app/lib）；接入 approval-dialog + menu 补 isPlainKey 防护（修 Ctrl+方向键误触发导航隐患）
  - **B3 遗留待办**：验收口径 18 处 <12px 等宽字号待核查（approvalKey 10.5px 等）、⑤ 精读 DSH packages/client/AGENTS.md 未排、纸感词汇注释未标退役
  - **2026-08-28 等宽口径收口（§10.5 落地）**：19 处 <12px 等宽字号全部提到 12px（10 个 CSS 文件，含 markdown th / code-copy / alarmTag / approvalKey / compactionTag / callTag 等）。核查依据：全部是信息承载元素（时间戳/键帽/徽章/提示/表头），无纯装饰豁免；若观感突兀改的是口径不是硬提字号（教学点：口径 vs 实现偏差，两种修法——让实现达标 or 修订口径）

- **2026-08-26 幽灵会话 bug 修复（删除会话没用的根因）**（回归测试 `lib/session/manager.test.ts`）：
  - 现象：侧边栏出现几个"0 条对话"的会话，点删除没用
  - 根因：`SessionManager.list()` 把 `.sessions/` 里所有 `*.jsonl` 都当会话列——**B1 审批日志（`<id>.approval.jsonl`，带点）和手动副本（文件名带空格）混进来**；这类 id 过不了 `isValidSessionId`（只允许 `[a-zA-Z0-9_-]`）→ DELETE 400 → 前端把 ApiError 吞了 →「列得出、删不掉」
  - 修法：`list()` 用 `isValidSessionId` 过滤（与路由层同一把尺，进出对称）；`use-sessions.remove` 的 catch 加 `console.warn`（错误可见，不再静默）
  - 教训：**「列得出」和「删得掉」必须用同一把合法性尺子**；目录即存储时，非会话文件（审计日志/副本）要能被列出层识别，否则就是幽灵数据
- **2026-08-26 会话列表保鲜 bug 修复（新建会话聊完仍显示"未命名会话 0 条"）**：
  - 现象：新建会话并对话后，列表第一条一直是"未命名会话 0 条对话"
  - 根因：会话列表只在 **挂载/新建/重命名/删除** 时 `refresh()`，run 结束后从不刷新 → 列表停在创建时快照（消息数/预览/排序全旧）；会话文件本身没问题（实测消息都写进去了）
  - 修法：`page.tsx` 用 `loading` 跳变（true→false = run 结束）触发 `refresh()`——用 loading 而非 done 帧判断，出错/停止导致的 run 结束也覆盖
  - 教训：**列表展示态和真实数据之间需要一个"内容变更后刷新"的触发器**；CRUD 触发刷新只覆盖了一半生命周期，内容（聊天）变更也要覆盖
- **2026-08-26 评审整改（用户要求四问：需求/硬编码/高内聚低耦合/重复代码）**：
  - ① 需求：本会话全部需求已覆盖（UI V2 + 中性壳 + 会话行 DSH 式 + 三个 bug 修复 + 审批卡移位 + 滚动吸底）
  - ② 硬编码：`#2c323c`（墨色 hover 加深）3 处 → 令牌 `--ink-hover`（globals.css）；`48px` 吸底阈值 2 处 → 收进 hook 常量
  - ③④ 高内聚/重复：滚动吸底逻辑从 page.tsx 内联 6 段 → **`useStickyScroll(ref)` hook**（`app/lib/use-sticky-scroll.ts`，判定 + 阈值封装，页面只剩两行组装）；`Menu` 原语本身高内聚未动
  - 验证：tsc 0 错 + 167 测试全绿；`#2c323c` 只剩令牌定义处、page.tsx 零处 48px 判定
- **2026-08-26 滚动跟随吸底修复（输出时滚动轴被强拽到底，看不了上面的内容）**：
  - 现象：流式输出时每次 delta 都强制 `scrollToEnd`，用户滚上去看历史会被拽回底部
  - 修法：`page.tsx` 加 `transcriptStickyRef/traceStickyRef`（吸底标记）——距底部 48px 内算"停靠"才自动滚；用户滚上去（超出 48px）停止跟随，滚回底部自动恢复。转录稿 + 轨迹区同款处理（后者通过 traceRef 绑 scroll 监听）
  - 教训：自动滚动必须"可被用户打断"；无条件 scrollToEnd 是聊天 UIs 的经典反模式，标准解法是吸底判定
- **2026-08-26 AI 开发页面的约束纪律**（用户问"他们怎么约束 AI 写页面"→ 写成显式规则，`doc/plan/ui-design.md` §10.6）：五层约束（令牌→组件→文档→代码即约束→流程人审）+ 硬规则「组件组合 > 发明」；B3 加精读任务：DSH `packages/client/AGENTS.md`（"Web client stack"，参考项目约束 AI 的教材）

## 精读/规划成果（workspace/ 笔记**不进 git**，检查点本文件**进 git**）

> 2026-08-25 调整：`会话检查点.md` 移到根目录并纳入版本管理——以后每个需求改完，**检查点 + PLAN.md 按需更新并随代码提交**（用户规矩）。workspace/ 下其余笔记仍不进 git。

| 类别 | 文件 | 状态 |
|---|---|---|
| 三项目精读 | `精读走读-01~17-*.md`（pi 7 课 / codex 5 课 / DSH 6 课） | ✅ 完成 |
| 产品端对照 | `精读走读-18-产品端面板对照.md`（tether + Reasonix） | ✅ 完成 |
| 决策矩阵 | `三项目横向对照-施工决策表.md`（抄谁 + 分歧裁决 + **第五节工具参考源**） | ✅ 完成 |
| 学习课程表 | `做中学学习主线.md`（学什么/做什么/读哪篇/验收/考核） | ✅ 完成 |
| 理论核心 | `理论核心-ETCLOVG落地.md`（七层坐标系 × 我们 × 走读 × 阶段） | ✅ 完成 |
| 项目对比 | `开源项目对比分析.md`（9 项目定位谱系 + 六维对比） | ✅ 完成 |
| 规划 | `三项目精读路线图.md`（阅读顺序 + 施工单 + 完成总结） | ✅ 完成 |
| **工具全景** | **`工具全景对比-7项目.md`（2026-08-24：7 项目工具名实读 + 缺口三档 + 排期）** | ✅ 完成 |

**精读结论**：核心抄 pi/codex/DSH，产品端 UI 抄 tether/Reasonix。规划已闭环，**接下来主线是"做"不是"读"**。

## 排期全景（2026-08-24 更新，PLAN.md 7.3（排期表））

> ⚠️ **以 `PLAN.md` 13.3 为准（单一事实来源），此处仅概览快照，可能过期。** 排期变化只改 PLAN.md，改完顺手同步本表。

```
A 主线收尾：A1 Phase5✅ → A2 vitest → A3 Trace Viewer → A4 ask_user_question → A5 web_search
B 工程化：  B1 审批 → B2 真摘要 → B3 原语库(UI V2 ①②✅) → B4 diff → B5 hooks → B6 记忆 → B7 终端加固
           → B8 前端单测（非核心，等 UI 冻结信号）
C 功能丰富：C1 MCP → C2 沙箱 → C3 子代理 → C4 schedule → C5 遥测 → C6 工作区 → C7 设置
           → C8 Skills → C9 代码执行器 → C10 goal → C11 complete_step → C12 move/read_image
非核心可选：LSP 代码智能 / tool_search / 前端单测 B8（条件触发，不优先）
```

> **2026-08-24 用户拍板的原则**：**"除非非核心功能，其余都应该安排到 plan 里面"**——之前"暂缓/不学"的措辞是 AI 建议、用户未拍板（用户质问"我什么时候说不学的"），已全部修正为排期；仅 LSP/tool_search 标"非核心/可选"。

## 模块地图（2026-08-26 B2 ③ + bash 乱码根治后）

- `lib/`：`types.ts`（协议 + SessionStats + **TodoItem/SessionEntry.todo** + **compactionSummary**）、`config.ts`（**唯一配置入口，provider 可扩展，压缩阈值/env 口子全集中**）、`model.ts`（TeachingModel）、`deepseekModel.ts`（真实适配器，流式 tool_call 分片累积）、`mockModel.ts`、`agent.ts`（引擎，**只传运行时参 signal/onChunk，maxTurns 兜底 16**）、`message.ts`、**`session/`（2026-08-26 从 sessionStore.ts/sessionManager.ts 迁入）：`store.ts`（JSONL 会话 + appendTodo + **prepareCompaction/commitCompaction/buildContext 折叠**）、`manager.ts`、`index.ts`（出口）、`store.test.ts`**、`trace.ts`、`toolApproval.ts`、`runControl.ts`、`userAnswers.ts`、**`summarize.ts`（B2 ③：三提示词 + serializeConversation + generateSummary，失败返回 null）**、`process-output.ts`（**2026-08-26：默认 UTF-8**）
- `lib/tools/`：一工具一目录——`index.ts`（ToolRegistry + 组装，**createToolRegistry(ws, hooks)**）、`types.ts`（**ToolHooks + ToolExecutorOptions 只含运行时参**）、`path-utils.ts`、`shared.ts`、`list/read/write/write-note/edit/delete/grep/find/bash/todo/ask-user/`（各 index.ts + index.test.ts）、`bash/bash-runner.ts`（**2026-08-26：cmd + chcp 65001 + pwsh -EncodedCommand，PWSH_PATH 支持**）、`bash/bash-runner.test.ts`（**新，7 用例**）
- `app/api/`：`chat/route.ts`（SSE 主路由 + 审批 + **每请求组装 registry + hooks 闭包 + 三步压缩接线 + compacting 帧 + maxTurns**）、`chat/approve/`、`chat/stop/`、`chat/ask-user/`、`sessions/route.ts`（**import 改 @/lib/session**）
- `app/components/`：`nameplate` `overture` `message-row`（**压缩卡片 `<pre>`→`<Markdown>`**）`trace-rail` `session-list` `approval-dialog` `task-panel` `ask-user-card` `confirm-dialog`
- `app/lib/`：`sse.ts`（帧类型，含 tool_todo / ask_user_request / **compacting**）、`use-agent-run.ts`（**compacting state**）、`use-sessions.ts`、`trace-fold.ts`、`format.ts`、`messages.ts`
- `doc/`：`01-agent-loop-纯净性改造.md`、**`02-B2-LLM结构化摘要-改造方案.md`（四家对照 + 10 决策）、`知识点-01-偏函数封装.md`、`知识点-02-todo面板.md`、`知识点-03-上下文压缩.md`、`知识点清单-本周回顾.md`（周五分享用）**

## 关键决策（确认过的，别再翻案）

1. **人工确认**：写/改/删 + bash **全量弹框**（`TOOLS_NEEDING_CONFIRM`），**5 小时超时自动拒绝（2026-08-25 从 60s 放宽，前端不展示时间文案）**；secret/秘密 硬拦；**"会话/全局允许"留待 B1**。
2. **bash 超时 30 秒**；停止走 `POST /api/chat/stop`。**2026-08-26：输出编码根治 = `cmd /c "chcp 65001 >nul & pwsh -EncodedCommand <base64>"`**（受限语言模式下 .NET 双 pin 被禁，chcp 是 cmd 内建不碰 .NET；base64 避开 cmd 引号解析）。**不再用 process-output 猜编码（默认 UTF-8）**。
3. **统计 = 会话级累计**（服务端从会话文件算），不是前端临时值。
4. 统计语义、UI、三元风格都听用户（"简单的三元保留、嵌套的拆"）。
5. **提示词「任务优先于教学」**：执行/验证先给结果，失败如实报错误+退出码。
6. 提交规矩（AGENTS.md 第 9 条）：**先展示清单 → 用户确认 → 提交**；`git add` 后必须 `git diff --cached --name-status` 验证暂存列表。
7. **Phase 5**：todo 是**会话事件**不是独立存储（`SessionEntry.todo`）；`todo_write` **整表替换**（幂等）；**2026-08-25 改**：工具不碰 store，hooks 走**工厂参数 + 闭包烙**（`createTodoTool(hooks)`，贴 pi），不再经引擎透传；**不进弹框**（低危）；前端**只读展示**。
8. **agent-loop 纯净性（2026-08-25，红线）**：引擎（`RunAgentLoopOptions`/`executeToolCall`）**不认识任何业务**；业务 hooks（onTodoWrite/onAskUser）走工厂参数闭包烙，引擎只传运行时参（signal/onChunk）。**改 agent-loop 必须先精读 pi → 对比 → 展示 → 用户判断，不许直接改**（AGENTS.md）。
9. **配置集中（2026-08-25）**：`lib/config.ts` 是唯一配置入口，引擎/工具不直接读 `process.env`；provider 可扩展（换厂商 = AI_PROVIDER + config 加一项 + 适配器），压缩阈值随 provider 的 contextWindow 走（对齐 pi shouldCompact）。**C7 设置页 UI 将来读写它**。
10. **压缩是"防爆窗"保险不是"勤打扫"**：DeepSeek 1M 窗口，阈值 = 窗口 - 预留（约 984K），几十上百轮才触发；演示压缩可临时调 env。
11. **工具排期原则（2026-08-24）**：默认都进 PLAN，非核心才不排；A4/A5/C9-C12 已排，LSP/tool_search 条件触发。
12. **流式 tool_call 机制**：arguments 是**分片累积**（`deepseekModel.ts`），流结束后一次 parse。**缺口：无截断保护**（`stopReason="length"` 时残缺 JSON 可能被 parse 成不完整对象去执行——pi 有 `failToolCallsFromTruncatedMessage`，我们没补，todo 靠 `validateTodos` 兜底）。
13. **DSH 式存储型自动标题——已否决（2026-08-26）**：曾讨论"首条用户消息截断写进文件头当正式标题"，实现后用户拍板回退（"和当前相差太大"）。结论：当前派生式标题（`title ?? preview ?? "未命名会话"`）已证明不会丢——用户消息在循环前落盘（route.ts:234）、文件只追加不删（压缩也不删旧消息）；存储型要动存储层（重写文件头）+ 路由 + 展示三层，收益不抵复杂度。将来若要做，先读这条。

## 遗留 / 待办（"还有好多没实现"清单）

| 项 | 状态 | 位置 |
|---|---|---|
| **A2 测试框架（vitest）** | ✅ 完成（15 文件 98 用例） | lib/*.test.ts |
| **A4 ask_user_question** | ✅ 完成并提交（`9de50d5`） | PLAN.md 7.3（排期表） |
| **B2 真摘要压缩**——**✅ 完成（2026-08-26）**：①摘要消息类型 `compactionSummary` ②切点不落 toolResult ③**LLM 结构化摘要（lib/summarize.ts + store 三步 + 前端 compacting 帧 + 卡片 Markdown）** ④阈值配置化（✅ config.ts） | ✅ 完成 | PLAN.md B2 补记 |
| **A5 web_search / web_fetch** | 排队 | PLAN.md 7.3（排期表） |
| **A3 L3 Trace Viewer** | 排队 | PLAN.md 7.3（排期表） |
| **B1 审批升级**（只读放行 + 分级 + 日志；`TOOLS_NEEDING_CONFIRM` 可配化） | 排队（最快见效） | PLAN.md 7.3（排期表） |
| **C7 设置页 / 配置整合**——`lib/config.ts` 骨架已建（✅），设置页 UI 读写未做 | 排队 | PLAN.md C7 前置 |
| 流式截断保护（`failToolCallsFromTruncatedMessage`） | 待办 | deepseekModel.ts |
| 工作区选择（完整版） | 暂缓 | PLAN.md 十一节 |
| UI 视觉打磨（确认弹框样式，用户不满意） | 暂缓 | PLAN.md 十一节 |
| fetch 封装（`app/services` 已部分做） | 暂缓 | PLAN.md 十一节 |
| 思考过程渲染（`reasoning_content`，适配器未捕获） | 暂缓 | PLAN.md 十一节 |
| edit_file 模糊匹配 / diff 预览（B4） | 排队 | PLAN.md 7.3（排期表） |

## 环境与验证经验（沙箱边界，别再踩）

- **沙箱起不了常驻 dev server**（`next dev` fork 报 EPERM）——约 30 秒短暂窗口可 curl 冒烟；**浏览器交互验证必须用户跑 `pnpm dev`**。
- **node 不能直接跑 TS**；临时脚本：tsc 编译到临时目录 + node 跑产物；顶层 await 包进 `main()`。
- **Windows 受限语言模式（ConstrainedLanguage）**：Smart App Control/WDAC 会禁 `.NET 类型创建/属性 setter/静态方法`——bash 输出编码不能用 preamble 双 pin（整行失败），正解是 `cmd /c "chcp 65001 >nul & pwsh -EncodedCommand <base64>"`。沙箱里同样受限，可直接验证该方案。
- **bash 输出编码**：统一 UTF-8（cmd 层 chcp + pwsh 启动读 65001），接收端固定 UTF-8 解；不再按平台猜 GBK。
- Windows：进程树要 `taskkill /t /f`。
- **子代理扫描残留**：调研时生成的 `.scan-*.txt` 等垃圾文件，用完即删。

## A5 web_search / web_fetch —— 搁置（2026-08-26 用户决定）

> **现状**：全部代码已回退（git restore + 删除），方案文档归档到 `doc/plan-archive/a5-web-search.md`（+ `.a5-notes-dsh.md` 调研笔记）。**PLAN.md 7.3 A5 行已恢复原状**。唯一保留的改动：**字体（layout.tsx/globals.css）——next/font/google 编译期访问 Google Fonts，国内网络不通会导致 dev 起不来，必须保留系统字体栈**。
>
> **为什么搁置**：实现过程走了大量弯路（IPv6 理论、缓存理论、UA、timeout 理论全被实测证伪），用户失去掌控感、对功能不了解，明确要求回退、后面再做。
>
> **已查明的技术事实（下次接手的起点，省得重新踩）**：
> 1. **web_search 服务端搜索本身可用**：DeepSeek Anthropic 兼容端点 `api.deepseek.com/anthropic/v1/messages` + `web_search_20250305` + 现有 DEEPSEEK_API_KEY，`scripts/verify-websearch.mjs`（已随回退删除，见归档文档）验证 HTTP 200 返回 web_search_tool_result 块。
> 2. **web_fetch 最后卡住的谜团**：用户终端（Node v24）和沙箱（Node v22.16.0）里 https.request + lookup 钉 IP 连 langchain-doc.cn 全部 HTTP 200；**但 dev server（Node v22）里 fetchUrl 30s 超时，而原生 fetch（undici）15s 内 200**。疑似 CDN 临时限流（请求太密集）或 dev 环境网络差异，**未最终定位**。
> 3. **踩过的硬坑（已定位）**：
>    - **Node v22 默认 autoSelectFamily（Happy Eyeballs）**，以 `all:true` 调自定义 lookup，回调**必须返回 `[{address, family}]` 数组**；返回单地址字符串会被按字符解构 → `ERR_INVALID_IP_ADDRESS(undefined)`（"Invalid IP address: undefined"）。Node v24 无此问题。**修复 = 检查 options.all 选约定**。
>    - **socket timeout 事件只在连接建立后的空闲期触发**；TCP 连接阶段挂起（SYN 被丢）时不会触发，必须用 setTimeout 强制 destroy。
>    - **环境差异优先对比 Node 版本**：终端 v24 vs dev v22 导致"脚本成功、项目失败"，绕了很多轮才意识到。
>    - SSRF 防护设计（DNS 预解析 + blockedFetchIP + lookup 钉 IP 防 rebinding）本身正确，可复用；`hostname` 保持域名（TLS SNI/Host 头），lookup 钉校验过的 IP。
> 4. **下次决策点**：a) 是否先解决"dev 环境 fetchUrl 超时 vs 终端 200"的谜团（建议：在 dev 里用调试路由对比 directRequest/nativeFetch/fetchUrl 三路）；b) 或改用 Node 原生 fetch（undici，天然多 IP Happy Eyeballs）+ 连接后校验实际 IP 的简化 SSRF 方案；c) 或换参考源（DSH 用 Node fetch，Reasonix 用 Go 的 http.Client，都非自定义 lookup 方案）。

## 未提交内容（工作区）

> 2026-08-26 收官：本批次 = **本次会话全部工作**。更早的 B1 审批 / B2③ 压缩 / 字体 / A5 归档均已提交（git log 53a4e5f/b272d37/ef5cc4c/72235d2/5108c17），不再列在此处。

- **UI V2：尺度升级 + 溢出菜单 + 中性壳**（核心批）：
  - 新原语：`app/components/ui/menu/`（Menu 下拉菜单：键盘导航/ARIA/危险项/侧开 fixed 定位）
  - 令牌：`app/globals.css`（--r 6px/--r-lg 10px/--hit 34px、正文 15px、**--ink-hover**、deck 264/384、**中性壳** #fff/#f8f8f8/#e4e4e4、**点阵纸背景删除**、死代码 .btn/.iconBtn 清理）
  - 组件：9 个组件样式上提 + nameplate/session-list 逻辑改（⋯ 菜单、hover 显示、无文件 id、去边框、侧开弹框）
  - 布局：`app/page.module.css`（880 列、32px 留白、发送/停止 40px、decisionBar）
- **清空会话功能删除**：nameplate（去 Menu + onReset/canReset）、page.tsx（去 reset 接线）、use-agent-run.ts（去 reset 保留 resetLocal）、services/chat（clearHistory 保留 + 注释）、api/chat/route.ts（DELETE 注释更新）
- **Bug 修复 ×3**：① 幽灵会话（`lib/session/manager.ts` list() 过滤 + `manager.test.ts` 新 3 测试 + `use-sessions.ts` console.warn）② 列表保鲜（`page.tsx` loading 跳变 → refresh）③ 滚动吸底（**`app/lib/use-sticky-scroll.ts` 新 hook**，评审整改）
- **审批卡移位**：消息流 → 底部决策区（page.tsx decisionBar 三选一 + approval-dialog 样式 + `b1-approval.md` 补记第 6 条）
- **文档**：`ui-design.md` §10.5-10.7（V2 决策/约束纪律/纸感退役）、`PLAN.md`（B3 ⏳ 进行中、**B8 前端单测新增·非核心**）、`b1-approval.md`、本检查点
- **workspace 笔记全部未跟踪**（用户决策：笔记不进 git）：18 篇精读走读 + 学习主线 + 决策表等
- **会话检查点本文件（进 git）**：随代码提交

## 常用命令

```bash
pnpm dev                                        # 开发（浏览器 localhost:3000）
node node_modules/typescript/bin/tsc --noEmit   # 类型检查（沙箱里 pnpm 被拦时用这个）
```

## 给下一个"我"的三句话

1. **架构状态**：Phase 0-5 + A4 + B1 审批 + B2 ③ 压缩全部完成并提交。**2026-08-26 UI V2 收官（B3 ①② 落地）**：Menu 原语 + 全面板 34px/15px 尺度 + **中性壳**（白中灰侧、纸感退役、点阵纸删除）+ 会话行 DSH 式（hover ⋯/无文件 id/无边框/侧开）+ 清空会话功能删除 + 三个 bug 修复（幽灵会话/列表保鲜/滚动吸底）+ 审批卡移到底部决策区。后端 lib/ 167 测试全绿，前端测试未做（B8 非核心，等冻结信号）。
2. **用户偏好**：可读性第一；UI 用户要自己调（本会话改了 3 次背景、加删了自动标题——**设计身份是用户需求不是 AI 发明**，给主题要标注"这是继承决定"）；提交前必须展示清单等确认；统计/产品语义先问用户；**"除非非核心功能，其余都排进 PLAN"**；**学习优先于产出**：功能做完先复盘、确认理解，别机械问 commit。**agent-loop 是红线**：改引擎必须先精读 pi → 对比 → 展示 → 用户判断。**计划增改/收尾走 AGENTS.md 第 10 条流程**。
3. **下一步**：B3 ③④⑤（面板 chrome 统一 / dialog·collapse·select 原语 / 精读 DSH `packages/client/AGENTS.md`）→ **C13 斜杠命令面板**（/compact 手动压缩口子等它落地）→ B8 前端单测（非核心，纯函数层 trace-fold/messages/format 随时可做，组件层等冻结信号）。提交：本次会话批次已收官，清单已展示，等用户确认后分批 commit。
